__pycache__/
*.py[cod]
*.log
*.bak
.venv/
venv/
dist/
build/
*.spec

# exe 바이너리는 직접 업로드 (빌드 결과물)
# images2/ 도 직접 업로드
# 단, .gitkeep 파일은 포함 (빈 폴더 유지용)

# 대시보드 비번(로컬 전용) — seed_server 자동 IP 등록용. 절대 커밋 금지
seed_secret.txt
.gh_tok
