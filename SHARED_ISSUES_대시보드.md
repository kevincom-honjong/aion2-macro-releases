# SHARED_ISSUES_대시보드.md — 대시보드 세션이 발견했지만 ★담당 경로(server/) 밖★ 이라 고치지 않은 것

작성: 대시보드 세션, 2026-09-12. 한 항목 = 파일 / 문제 / 재현 조건 / 제안 네 줄.
다른 세션은 읽기만. 이 파일은 대시보드 세션만 쓴다.

---

## 1. 매크로가 명령을 ★실행 전에★ ack 한다 → 서버·대시보드가 「전달됨」을 「실행됨」으로 읽는다
> **처리 완료: 2026-09-12 서버 `ack_cmd` 본문 `{status,why}`→cancelled + 매크로 `notify_dropped`(부팅 드레인·오버라이드·사고 523 자리). 시험 I1·게이트 52. INTEGRATION_LOG C1**
- 파일: `src/lc/report_module.py` (WS `on_message` 의 ack, HTTP 폴링 drain 의 ack) · `src/lc/loot.py` (`drop_pending_commands`, `enqueue_command`)
- 문제: 매크로는 받자마자 ack 를 보내고, 그 뒤 새 사람 명령이 오면 `drop_pending_commands` 가 대기 큐를 통째로 버린다(주인님 오버라이드 지시라 설계). 서버는 이미 `acked` 라 재전송도 없고 대시보드에도 아무 표시가 없다 = ★유실이 안 보인다★.
- 재현 조건: WS 재접속 직후 서버가 밀린 명령 2건 이상을 한 묶음으로 보낸 뒤 4초 안에 사람이 다른 명령을 누른다. 뒤 명령이 앞 묶음의 나머지를 버린다.
- 제안: 매크로가 「버렸다」를 서버에 알리는 창구를 쓴다 — 서버 `POST /command/{pc}/ack/{id}` 에 `{"status":"cancelled","why":"..."}` 를 받게 넓힐 준비가 돼 있다(대시보드는 이미 `cancelled` 를 ⛔ 로 그린다). 매크로 쪽에서 `drop_pending_commands` 가 버린 id 마다 그걸 부르면 끝난다. 서버 쪽 확장은 CONTRACTS_대시보드.md #1.

## 2. 매크로가 ⛔ 알람 중계 응답의 `ok:false` 를 보지 않는다
> **처리 완료: 2026-09-12 매크로 `_tg_body_ok`(send·photo). INTEGRATION_LOG C2**
- 파일: `src/lc/report_module.py` `telegram_send` (`return bool(r.status_code == 200)`) · `src/lc/config.py` (「[텔레그램] 중계 전송」 로그)
- 문제: 서버가 음소거로 생략하면 이제 `{"ok":false,"reason":"muted"}` 를 준다(2026-09-11 변경). 매크로는 HTTP 200 만 보고 성공으로 찍는다 → 로그가 「보냈다」로 거짓말한다.
- 재현 조건: 대시보드에서 PC 를 음소거한 뒤 그 PC 가 일반 알람을 보낸다. 서버 로그엔 「중계 생략(음소거)」, 매크로 로그엔 「중계 전송」.
- 제안: `telegram_send` 가 본문의 `ok` 를 보고 False 를 돌려주게. ⛔·🚨 는 서버가 음소거를 뚫으므로 그쪽은 영향 없다.

## 3. HTTP 로그 폴백이 51번째 줄부터 잃는다 — 매크로가 버퍼를 먼저 비운다
> **처리 완료: 2026-09-12 매크로 `_flush_logs` 50줄씩 + dropped 경고(`LOG_BATCH_MAX` 짝은 verify_all C). INTEGRATION_LOG C3**
- 파일: `src/lc/report_module.py` `_flush_logs` (`_log_buffer.clear()` 뒤 `_post`)
- 문제: 서버는 배치당 50줄만 저장하고 이제 `{"count":50,"received":70,"dropped":20}` 로 정직하게 답한다(2026-09-11). 매크로는 post 전에 버퍼를 비우고 응답을 안 보므로 버려진 줄은 어디에도 없다. 이 경로는 WS 가 끊긴 구간 전용 = ★사고 구간의 로그가 정확히 여기서 잘린다★.
- 재현 조건: WS 가 60초 이상 끊긴 채 로그가 51줄 넘게 쌓였다가 재접속.
- 제안: 배치를 50줄씩 나눠 보내거나, 응답의 `dropped` 가 0 이 아니면 그 줄들을 다시 버퍼에 넣는다.

## 4. `web/server/main.py` 미러가 낡는다
> **처리 완료: 2026-09-12 미러를 없앴다(`web/server/main.py.mirror_removed_20260912` 보관), 문서 5곳 정본 경로로. verify_all C 가 부재를 지킨다. INTEGRATION_LOG D4**
- 파일: `src/web/server/main.py`
- 문제: 배포되는 정본은 `src/updater/server/main.py` 다. `web/server/main.py` 는 손으로 복사하는 미러인데(HANDOFF 규칙) 대시보드 세션은 이번 작업부터 담당 경로 밖을 안 고치므로 ★이 미러가 낡은 채로 남는다★.
- 재현 조건: 2026-09-12 이후 `diff src/updater/server/main.py src/web/server/main.py`.
- 제안: 미러를 버리거나(정본 하나면 충분하다), web 쪽 도구가 필요할 때 `updater/server/main.py` 를 직접 읽게 한다.

## 5. 관제컴 `parsec_multi.py` 가 API 키로 주소록을 덮어쓴다
> **처리 완료: 2026-09-13 주인님 「더 쉽고 확실한 방법」 → 세션 로그인만. 도구는 seed_secret.txt 비번으로 /auth/login, 서버 POST 는 check_session 만(API 키 401). INTEGRATION_LOG D5**
- 파일: `src/updater/parsec_multi.py` (382행 근처, `X-Api-Key` 로 `POST /parsec/map`)
- 문제: 매크로 API 키는 공개 exe 에 각인돼 유출 전제다. 그 키로 파섹 주소록(peer_id)을 덮을 수 있으면 다음 계정전환이 ★공격자 호스트로 접속★ 한다. 서버 쪽에서 세션 전용으로 바꾸면 이 도구가 깨진다.
- 재현 조건: 유출 키로 `POST /parsec/map {"map":{"8":"<남의 peer_id>"}}`.
- 제안: CONTRACTS_대시보드.md #2 — 쓰기 전용 별도 토큰 또는 세션 로그인으로 바꾸는 계약. 그 전까지 서버는 읽기·쓰기 모두 API 키를 허용한 채로 둔다.

## 6. 업데이터 명령은 만료·중복 방지가 없다
> **부분: 2026-09-12 클라이언트 `_done_cmd_ids` dedup + ack 실패 로그(시험 cmd_dedup_test). 서버 만료는 주인님 결정. INTEGRATION_LOG C4**
- 파일: `src/updater/client/updater.py` (폴링 → `handle_command`, ack 실패를 `except: pass`) · 서버 `database.py get_pending_updater_command`
- 문제: ack POST 가 실패하면(서버 굳음·재배포 창) 10초마다 같은 `update` 를 다시 실행한다(stop→start 반복). 꺼진 PC 에 넣은 `update` 는 며칠 뒤 부팅해도 실행된다. 매크로 큐엔 15분 만료·id dedup 이 있는데 업데이터엔 둘 다 없다.
- 재현 조건: 업데이터가 명령을 받은 직후 서버가 재배포된다.
- 제안: 서버가 만료(15분)와 `delivered_at` 표시를 넣을 수 있지만 「꺼진 PC 가 켜지면 밀린 업데이트를 받는다」가 의도된 동작일 수 있어 주인님 결정이 필요하다. 클라이언트는 최소한 같은 id 를 두 번 실행하지 않게.

## 7. 공용 exe 안의 비밀 11개 (게이트 `secret_audit` 빨간불)
> **처리 완료: 2026-09-13 주인님 「키는 프로그램에 박는다」 — 게이트 20 면제 표식. INTEGRATION_LOG D3**
- 파일: `src/updater/exe/혼종_통합_자동.exe` · `src/lc/config.py` (secrets_default)
- 문제: 공개 저장소에 올라가는 exe 에서 API 키·텔레그램·OCR 키가 추출된다. 서버는 그래서 API 키를 「유출 전제」로 다룬다. 대시보드 쪽에서 막을 수 없다.
- 재현 조건: `python -X utf8 secret_audit.py --gate` (web/.claude/ops).
- 제안: 이미 NOW.md 0-L 에 있는 그대로 — 빌드 때 secrets_default 를 비우고 각 PC info.txt 로 키를 넣는다. 24대 순서가 있어 주인님 몫.

## 2026-09-21 — ★옛 서버 로그에 계정 비번·PIN 이 평문으로 남아 있다★ (아이온2방 → 대시보드방, 주인님 지시)

> 세션 간 메시지로 먼저 보냈으나 ★승인 대기로 묶여 전달되지 않았다★ — 그래서 파일로 남긴다.

### 발견
2026-09-20 주인님 지시로 PC-20 비번을 바꾸고, §A2 대로 「200 은 전달이지 적용이 아니다」를 확인하려고
그 PC 로그를 읽다가 봤다:
```
[WS] 명령 수신: set_info (args={'kv': {… '계정1_비번': '<평문>', '계정1_PIN': '<평문>' …
```

### 매크로 쪽은 고쳤다 (1.1.981 배포 완료, 사고 612)
마스킹은 2026-08-16(파섹 비번 사고) 때 만들어 배선까지 돼 있었는데 **한 겹만** 봤다.
1. `set_info` 는 §B3 규약상 args 를 `{"kv": {...}}` 로 감싼다 → 바깥 열쇠가 `kv` 라 힌트에 안 걸리고 값이 통째로 찍혔다.
2. 힌트가 영어뿐(`pw,pass,secret,token,api_key,apikey`)이라 `계정1_비번`·`계정1_PIN` 은 한 겹이었어도 못 걸렀다.

→ `lc/config.py::_mask_obj`(신설)·`mask_args`, `lc/report_module.py` 같은 짝을 **재귀(깊이 6)** 로 바꾸고
힌트에 `pin`·`비번`·`비밀번호` 를 넣었다. 값은 자리수만 남긴다(`***(11자)`). 서버·아이디 등 진단용 칸은 그대로.
하네스 `runtime_verify [2-aj]` 4검사로 고정.

**대시보드가 보여주는 명령 이력은 이미 마스킹돼 있다** — 2026-09-21 `/commands/recent` 에서 `"parsec_pw": "***"` 확인.

### ★부탁 — 대시보드 영역에서만 할 수 있는 일★
1.1.981 **이전에 이미 서버 DB 에 쌓인 로그 줄**에는 평문이 그대로 남아 있다. 매크로를 고쳐도 과거 기록은 안 지워진다.
- (a) 로그 테이블에서 `set_info` + `_비번`/`_PIN`/`parsec_pw` 패턴을 찾아 마스킹하거나 그 줄만 지울 수 있는지
- (b) 지우면 되돌릴 수 없으니 **주인님 확인 뒤** 진행할 것. 아이온2방은 서버 DB 를 건드리지 않는다(§F — 영역 밖)
- (c) 서버가 수신 args 를 다른 곳에도 남기는 경로(감사 로그·백업 등)가 있으면 같이 볼 것

**대시보드방 답(2026-09-21)** — 72카드+업데이터 로그 298,009줄 전수 스캔: `set_info` 포함 줄 0 · `parsec_pw` 217줄 전부 `***`.
PC-20 당시 줄은 보존 정리(3000줄/PC)로 이미 밀려나 없음 → (a) 지울 것 없음. 부수로 `/logs?limit=` 인자화(`445e3cd`, code `cd696d8b`).
★미확인★ Railway 볼륨의 백업·스냅샷에 옛 줄이 남았는지 — 세션 권한 밖. **주인님 몫**: Railway 콘솔 → 볼륨 → Backups 에 08~09-20 스냅샷이 있으면 지울지 결정.

### 참고 — 게이트 38 을 사고 611 결정대로 뒤집었다
`ROT_HUNT_MAX`(사냥 14시간 상한)는 **이제 순환을 세우면 안 된다**(주인님 2026-09-18·19 두 번 지시).
`rotttl_test` 가 그걸 지킨다 — 서버 `_rot_stop` 쪽을 만질 때 참고.
나머지 그물(`ROT_SWITCH_MAX`·`ROT_START_MAX`·`ROT_MAX_HOPS`·정보수집 상한)은 그대로 살아 있어야 한다.

### 아이온2 오늘 릴리스
1.1.981(612 비번 마스킹) · 982(613 로그인 페이지 로딩 대기) · 983(614 판매 진입 진단) ·
984(615 1차 실패 알람 억제) · 985(616 게임 없는 화면에서 판매 클릭 금지)

## 2026-09-23 — 팜뷰 전광판이 「모름(null)」을 0 으로 · 가짜 PC 를 효율 평균에 섞음 (대시보드방 → 팜뷰, 통합 세션 몫)

대시보드방이 반증 2바퀴에서 찾았다. 서버·대시보드 쪽은 고쳤다(미배포). **팜뷰 코드는 대시보드방이 안 고친다.**

1. `farmview/fvdash.py` `slim_global` — `"corridor_left": num((gl.get("totals") or {}).get("corridor_remaining"))`
   → 서버는 회랑 스냅샷이 하나도 없으면 `corridor_remaining: null`(FV_API.md 2026-09-23 절). `num(None)` = 0 이라
   팜뷰는 「0」, 대시보드는 「–」. 제안: `None` 이면 그대로 `None` 을 넘기고 UI `n0()`(이미 null → 「—」)에 맡긴다.
2. 같은 함수가 거래키나·오드·각성전 합계를 **카드별 정수(없으면 0)** 로 다시 더한다 — 서버 `global.totals`
   (trade_kina·odd_energy·awakening_ticket, 모르면 null · 은퇴/계정없음/가짜 제외)와 어긋날 수 있다.
   제안: 합계는 `global.totals` 를 그대로 쓴다(FV_API.md 「팜뷰는 global.totals 를 읽을 것」).
3. `eff` 평균에 PC-TEST·PC-DEMO(가짜)가 섞인다 — 대시보드는 2026-09-23 부터 `isExcludedPc` 로 뺀다.
   재현: 카드 PC-01 효율 10 + PC-DEMO 90 → 팜뷰 50.0, 대시보드 10.0.

## 2026-09-23 (밤) — 대시보드 B 묶음이 남긴 것: 계약 제안 · 다른 영역 수정 · 주인님 결정 (대시보드방 → 아이온2·통합)

서버 쪽은 전부 코드에 넣었다(미배포, `verify.py` 초록). 아래는 **대시보드방이 직접 못 고치는 것**만.

### 계약 제안 (FV_API.md 두 사본 — 통합 세션 결정)
- **P1** `/api/fv/events` 한 초에 limit 개 넘는 이벤트는 초 단위 커서로 못 나눈다 → 선택 파라미터 `after_id`(또는 `cursor`) + `next_cursor` 추가 제안(옛 클라 호환).
- **P2** 업데이터 로그 `at` = 제때 온 줄은 업데이터가 찍은 시각, 늦은 줄은 「서버가 받아 보이게 된 시각」(원래 시각은 메시지 끝 `(찍힘 HH:MM:SSZ)`) — 문서화만.
- **P3** events 는 최근 2초(`FV_EVENT_SETTLE_S`)를 다음 폴링으로 미룬다 + 아직 저장 중인 업데이터 줄(`_FV_INFLIGHT`)의 시각 앞에서 멈춘다 → 최대 약 3초 늦게 보인다 — 문서화만.
- **P4** `/api/fv/updcmd` id 가 벽시계 ms(≈1.79×10^12)에서 시작 — 예시 `1` 만 실제와 다르다 — 문서화만.
- **P5** `POST /api/fv/updcmd/ack` 에 정수 `id` 가 없으면 400 — 에러 표에 한 줄.
- **CQ5** 같은 restart/update 가 업데이터 큐와 팜뷰 큐로 두 번 실행될 수 있다 → ① `upd_cmd_id` 공유 + claim(먼저 받은 쪽만) 또는 ② 팜뷰 큐는 업데이터가 90초 안 가져간 것만. 팜뷰 계약·통합 시험 1건 필요.

### 아이온2(lc) 쪽 수정 메모
- **TG7** 서버가 음소거 PC 에 `{"ok":false,"reason":"muted"}` 를 주면 매크로가 「중계 실패」로 보고 PC 자체 봇으로 폴백 → 음소거가 뚫린다. `reason=="muted"` 는 처리됨으로 보고 폴백하지 않게.
- **CQ1** 매크로 부팅 때 `drop_pending` 이 `set_slot_filter`/`set_info` 를 버린다(서버는 이미 acked 라 재전송 없음). 서버는 `set_info` 를 `COMMAND_NO_EXPIRE` 에 넣었다 — 매크로 쪽 `BOOT_KEEP` 과 짝을 맞출 것.
- **char_info 「방금 읽음」 표식** — 매크로는 `merge=True` 로 로컬 스냅샷 통째를 보낸다(`info_collector` :1938·:2747, `awakening` :757, `surface_zero` :69, 재접속 재전송). 서버는 이제 「값이 바뀐 캐릭만」 수집 시각을 올린다. 초기화 뒤 같은 값(0→0)을 다시 읽은 슬롯은 여전히 초기화 보정을 받는다 — 닫으려면 슬롯별 `read_at` 을 실어 보내야 한다.
- **`web/.claude/ops/02_서버_대시보드.py:1517`** 의 `_CAP` 사본이 서버와 다르다(서버는 `ai_kina_sold: 4000` 추가). ops 스크립트는 대시보드방 경로가 아니다.

### 주인님 결정 대기
- **ROT5** 계정이 05:00 직전에 완주하고, 05:00 뒤 첫 순환 틱에 idle 이면 `_rot_done` 이 영원히 False(매크로는 새벽에 진행도만 리셋하고 스스로 시작하지 않는다, `lc/slot_manager.py:526`) → 조용히 멈춘다. 틱 1번(30초)+유예 창, PC 당 하루 한 번. 줄기(순환 판정) 수정이라 05:00 경계에서 어떻게 할지 결정 필요.
- **PageUp 핫키** 가 `_rot_arm(full=True)` 로 무장(대시보드 ▶시작은 full=False) → 「안 가 본 계정 먼저」 순서로 돈다. 의도인지.
- **DB3** 업데이터 명령 10분 만료 + 같은 명령 중복 대체 — 넣었다(미배포). 업데이터 클라 주석에 「주인님 결정 대기」 로 적혀 있던 항목이라 확인 필요.
- **악몽 티켓 상한** `NIGHTMARE_TICKET_MAX` — 값 대기(지금 null 이면 화면 「?」).

## 2026-09-23 (밤2) — 팜뷰 반증 셋 · 명령 시간초과 · 창고키나 장부 (대시보드방 → 아이온2·통합·팜뷰)
코드는 넣었다(미배포). 계약 문서는 안 고쳤다 — 아래를 통합 세션이 `FV_API.md`(정본)·`farmview/docs/FV_API.md`(사본)·`CONTRACTS_팜뷰.md` 에 옮길 것.

### FV_API 계약 변경 제안
- **FC1 (CQ5 닫음) 업데이트/재시작은 한 길로만** — 정본 = `FV_API.md` «2026-09-23 (밤) 추가 › B» (배포 반증 1·2·3차 반영). 요점: 팜뷰가 집으면 짝 업데이터 행 `fv_claimed`(업데이터에 안 나감) · 업데이터가 먼저 집으면 팜뷰 목록에서 빠짐 · 업데이터가 같은 종류를 90초 안에 받아 갔으면 다시 누른 것도 업데이터 몫 · ack 는 팜뷰 id 로 DB 에서 짝 행을 찾음 · `ok:true`→`fv_done` · 안 간 게 확실한 실패(`reached:false`·연결 거부·없는 PC·nobulk·에이전트 없음)만 업데이터로 한 번 · 그 밖의 실패 `fv_failed` · ★ack 없이 조용하면 업데이터에 넘기지 않고 `fv_unknown`(PC 로그 WARNING, 늦은 ack 가 닫음)★ · 재배포 뒤 첫 목록에서 DB 로 다시 세움(같은 id) · 덮어쓰기는 같은 종류만. 응답 모양 `{id,pc,act,at}` 그대로.
  - **FC1-4 팜뷰에 부탁 (배포 반증 3차 #4, 팜뷰 코드)** — ① `UPD_DONE`/`UPD_SINCE` 를 디스크에 남긴다(지금 메모리만 → 팜뷰가 ack 전에 재시작하면 `since=0` 으로 다시 받아 ★재실행★, 서버는 목록에 계속 내놓는다 — ack 전까지 나오는 게 계약이라 서버에서 못 막는다). ② ack 전송이 실패하면 재시도한다(서버 재배포 중 ack 가 사라지면 id 가 커서 이하라 목록에 다시 안 나와 `fv_unknown` 으로 끝난다). ③ ack 에 `reached` 를 명시한다(문구 추측 대신).
    - (2026-09-23 밤, 반증 3차 H6) ②의 서버 쪽 절반은 넣었다(미배포) — 집힌(ack 전) 명령은 `since` 와 무관하게 매 목록에 나온다. 그래서 ★팜뷰 `UPD_DONE` 이 그 id 를 기억하는 것이 이제 재실행을 막는 유일한 자물쇠다★. ④ `fvdash.pull_updcmd` 의 «500개 넘으면 `UPD_DONE.clear()`» 는 지울 것 — 비우는 순간 아직 ack 못 한 집힌 명령이 다시 실행된다(재시작 두 번). 오래된 id 부터 버리는 크기 제한(예: 순서 있는 dict 2000개)으로. ⑤ `UPD_SINCE` 는 «ack 성공한 가장 큰 id» 말고 «ack 못 한 가장 작은 id − 1» 까지만 올리면 서버 H6 없이도 맞다(둘 다 있으면 더 안전).
    - ⑥ (반증 v2 #2) 서버가 다시 주는 항목엔 `redeliver:true` 를 붙인다(더하기만, FV_API §B). 팜뷰는 그 id 를 `UPD_DONE` 에 기억하지 못하면(재시작) ★실행하지 말고★ `{ok:false, reached:true, why:"재시작해 결과 모름"}` 으로 ack 할 것 — 지금은 재시작한 팜뷰가 이미 돈 재시작을 한 번 더 돌린다. 서버는 `fv_unknown` 이 된 명령을 목록에서 빼고 재배포 뒤에도 다시 안 세운다(그 쪽 절반은 넣음·미배포).
    - ⑦ (2026-09-24 v2 반증 1부) `POST /api/fv/command` 함대 가드가 ★15분 굴러가는 창★ 이 됐다(FV_API «통합 계약 5건» 3번). 한 대씩 눌러 확인 안 된 PC 가 8대째가 되면 400 — 팜뷰는 그 400 문구(«함대 규모입니다»)를 보면 사람에게 확인을 받아 같은 요청을 `confirm_fleet:true` 로 다시 보낼 것. 지금 `fvdash.send_cmd` 는 한 요청이 8대 이상일 때만 붙인다. 정지 쪽·보기 전용은 창과 무관.
    - ⑧ (2026-09-24 v3 델타 반증 #6) 팜뷰 `dashapi.json` 의 `timeout`(사람이 바꿈, 최소 1)을 ★11초 아래면 11초로 올려★ 쓸 것 — 서버 `FV_CMD_DEADLINE_S`=8초 + 여유 3초. 더 짧으면 서버가 «닿았는지 모름» 을 답하기 전에 끊겨 «실패» 로 보이고 사람이 다시 누른다. 서버 시험 M16 은 `fvdash.py` 기본값(12초)만 본다 — 관제컴 설정 파일은 서버가 못 본다.
    - ⑨ (2026-09-24 v3 델타 반증 #1, ★서버 쪽 고침·미배포★) 모든 `/api/fv/*` 에러에 `ok:false`·`err` 를 더했다 — 5.54 는 이제 함대 창 400 을 실패 토스트로 그린다. 팜뷰 코드는 안 고쳐도 된다(기록만).
- **FC2 `FV_UPDCMD_SEQ` 재배포 0 초기화 — 선택: 디스크 대신 벽시계 ms** (B-FV4, 이미 들어 있음). id = max(앞 id+1, 지금 ms) → 재배포 뒤에도 항상 커진다. 부팅 id 는 안 붙인다. 문서의 예시 id `1` 을 ≈1.79×10^12 로(P4 와 같음).
- **FC3 `POST /api/fv/command` 시간 상한 9초(`FV_CMD_DEADLINE_S`, 팜뷰 12초보다 짧게)** — 응답에 `timeout: bool` 추가. 시간초과면 `ok:false · partial:true · timeout:true`, `results[]` 에 PC 별로: 끝남 = 예전 모양 / 시작했는데 안 끝남 = `ok:null, unknown:true`(뒤에서 끝까지 넣는다 — ★다시 누르지 말 것★) / 시작 못 함 = `ok:false, sent:false`(다시 보내도 됨). 제시간이면 예전과 같다(`timeout:false` 만 더). 팜뷰 쪽: `unknown` 은 «닿았는지 모름», `sent:false` 만 «실패» 로 그릴 것(`fvdash.py:590-605`·`h_cmd`).
- **FC4 (주인님 #104 어비스 카드용, 예고)** FV snapshot PC 에 `abyss_kina_state/gain/rate/since/mins` 통과 — 코드 넣을 때 따로 적는다.

### 아이온2(lc) 쪽 요청
- **KF1 창고키나 판독 표식 (P0 v3 — 아이온2 의 collect_seq 요청과 ★하나로★ 묶음, 프로그램방이 구현)** — `POST /char_info/{pc}` 본문에 두 칸:
  - `kina_read_at`: 창고키나를 ★실제로 읽은★ 시각, UTC `YYYY-MM-DDTHH:MM:SS`(오프셋을 붙이면 서버가 UTC 로 옮긴다). ★새로 읽은 경로만 새 값★(전체수집 `_collect_kina`·사고 569 `refresh_total_kina_after_deposit`). 로컬 `char_info.json` 에 같이 저장해서 ★재전송·merge·15분 재수집(`_resend_last_full`, 사고 594)·형제 재전송은 그 값을 그대로★ 보낸다.
  - `kina_seq`(선택): 카드별 단조 증가 정수(새로 읽을 때마다 +1, 로컬에 저장·재전송은 그대로).
  - 서버 규칙(`database.upsert_char_info`, 코드 넣음·미배포): `kina_read_at` ≤ 마지막으로 받아들인 판독 시각(또는 read_at 없이 `kina_seq` ≤ 마지막 순번) → ★창고키나를 안 덮는다★(캐릭 정보는 저장). 새 판독이면 `total_kina + Σ(그 판독 시각 ★뒤★ 기록된 팜뷰 차감)` 을 저장(보내기 실패 뒤 늦게 도착한 판독, 반증 ③). 표식 없는 옛 매크로는 장부 사슬(값 == 72시간 안 판매의 before → 사슬 끝, 틀리면 ★덜★ 보이는 쪽).
  - 앞서 적은 `kina_fresh:true` 제안은 ★철회★ — 위 두 칸으로 대신한다(서버도 안 받는다).
  - 시각: 매크로 `report_module._now()` 는 UTC(운영 실측 PC-10c collected_at 07:46:28 = 로그 16:46:28 KST). 매크로 시계가 서버보다 앞서면 그만큼 판독 직후 판매를 놓친다 — 시계 동기 전제.
  - (v2 반증 1부, 2026-09-24 — 위 «급하지 않다» 는 틀렸다, 다시 ★P0★) 서버가 카드별 마지막 어긋남 하나를 배워 merge·재전송에 쓰던 v2 는 반증자가 깼다: NTP 로 시계가 고쳐진 PC 의 merge 판독이 두 번 빠지거나(400, 정답 700) 버려져 얼었고, 엉터리 보고 하나(+1일·−1일)가 배운 값을 뒤집었고, 재배포로 배운 값이 사라지면 판매 전 판독의 재전송이 차감을 되살렸다(1000, 정답 700). 서버는 이제(코드 넣음·미배포) 요청마다 그 요청 안의 증거만 쓴다 — `sent_at` › (merge·재전송 아님) `collected_at` › (재전송 아닌 merge 가 창고키나를 실으면) `kina_read_at`. ★재전송만은 본문에 증거가 없다★ — 최근 2시간 표본 3개가 60초 안에서 맞을 때만 배운 값을 쓰고(표 `pc_clock`, 재배포에도 남음) 아니면 PC 시계를 믿는다. 그래서 ★부팅 직후·시계가 막 고쳐진 뒤의 재전송★ 은 여전히 PC 시계다(판매 전 판독이면 차감이 되살아날 수 있다). 이 틈은 매크로가 아래 `sent_at` 을 보내야만 닫힌다.
  - **★P0★ KF1-b `sent_at` 한 칸 더 (2026-09-23 밤 배포 반증 D #4 · 2026-09-24 v2 반증 1부로 P0 — 프로그램방 구현 부탁)** — ★모든 POST★(`send_char_info` 의 새 판독·merge·15분 재수집, `_resend_body` 로 보내는 부팅/WS 재전송·형제 재전송 전부)가 ★보내는 그 순간★ 의 `_now()`(같은 PC 시계, UTC `YYYY-MM-DDTHH:MM:SS`)를 `sent_at` 으로 싣는다. 로컬 char_info.json 에는 ★저장하지 않는다★(저장하면 재전송이 옛 sent_at 을 싣는다 — `_post` 직전에 본문에만). `kina_read_at` 은 반대로 그대로(실제로 읽은 시각). 시험: 재전송 본문에 sent_at 이 있고 그 값이 보내는 순간이며 json 파일엔 없는지. 서버는 `판독 시각 + (서버 수신 − sent_at)` 으로 PC 시계 어긋남을 빼고 판매 순서를 가른다(`database.kina_read_judge`, 코드 넣음·미배포). 없으면 PC 시계를 그대로 믿는다(옛 동작 — PC 시계가 10분 빠르면 재전송 때 차감이 되살아난다, 반증 D 실측 1000 vs 맞는 값 700).
  - **KF1-c `kina_read_at` 은 창고를 ★실제로 읽은 순간★ 에 (2026-09-24 대시보드 반증 에이전트 #5 — 프로그램방, ★KF1-b 와 같은 판에만★)** — 지금 `_stamp_kina` 는 보내기 직전 `_now()` 를 찍는다. 전체수집(`info_collector.collect_and_send`)은 1번 슬롯에서 창고를 읽고 몇 분 뒤에 보내므로, 그 사이 기록된 팜뷰 판매(`kina_adjust`)가 «판독보다 옛것» 으로 보여 차감이 되살아난다(실측 1000, 맞는 값 700 — 서버는 가를 수 없다). `_collect_kina` 가 읽은 순간의 `_now()` 를 들고 있다가 표식으로. ★주의★: 서버는 `sent_at` 이 없는 merge 에서 `kina_read_at` 을 «보낸 시각» 증거로 쓴다(지금은 둘이 같은 순간이라서) — 그래서 이 변경은 ★모든 POST 에 sent_at 을 싣는 KF1-b 와 같은 exe 에서만★. 따로 내면 merge 판매 판정이 틀어진다.
  - 서버 거절(400, 코드 넣음·미배포): `total_kina` bool·음수·10^15 초과·32자 넘는 문자열(v3 델타 반증 #2 — 5000자리가 500 이었다) / `kina_seq` 정수 아님·bool·음수·2^53 이상 / `collected_at` 문자열 아님. `kina_read_at`·`sent_at` 형이 틀리면 없는 것으로 친다. 판독 시각이 `sent_at`(없으면 서버 시각)보다 5분 넘게 뒤거나 72시간보다 옛것이면 창고키나를 안 덮고 그 PC 로그에 «… PC 시계를 확인하십시오» 한 줄.

## 2026-09-24 — v3 델타 반증 #5: PC 시계가 하루 넘게 느리면 창고키나가 두 번 빠진다 (★설계상 — 기록만★)
- 반증 X5: PC 시계가 2일 느린 카드가 새로 읽은 700(판매 300 이 이미 빠진 값)을 보내면 400 으로 저장된다. 서버는 하루(`PC_CLOCK_OFS_MAX_S`) 넘는 어긋남을 증거로 안 쓰고(엉터리 보고 하나가 배운 값을 뒤집던 v2 반증 1부 A-1) 그 PC 시계를 그대로 믿으므로, PC 가 찍은 판독 시각이 판매보다 옛것으로 보여 판매를 한 번 더 뺀다.
- 막는 길은 매크로 쪽뿐 — KF1-b `sent_at`(같은 PC 시계로 보낸 순간)이 오면 판독 시각과의 차가 PC 시계와 무관해져 닫힌다. 하루 넘게 틀린 시계는 Windows 시간 동기가 꺼진 PC 이므로 운영에서 시간 동기를 켜 두는 것이 1차 방어.
