# server/HANDOFF.md — 대시보드 영역 인수인계 (2026-09-12)

먼저 `CLAUDE.md`(같은 폴더)를 읽고, 그다음 이 파일. 상태 정본은 `../../web/.claude/ops/NOW.md` 0-AB·0-AC 절(더 길다).

## ★지금 상태 (2026-09-23 밤) — 세션이 끊기면 여기서 다시 시작★
- **★나가는 바이트 계기판 (2026-09-26 주인님 «레일웨이 결제 비용이 왜이러냐» — Network $51.62 ≈ 1TB/월) — 코드에만·미배포★** `main._EgressMiddleware`(HTTP 본문·WS 보낸 글자, 경로 묶음 `_egress_key`, 오래 사는 WS 는 1MB 마다) · `GET /diag/egress`(main 세션 — 경로·`/img` 파일별·`/check` 가 권한 이미지별 `check_offer`·텔레그램 사진 `out`, 시간당·월 GB·$) · `/img` 가드: 상류 해시 ≠ version.json 이면 503(작게, 캐시 안 함 — 업데이터 sha 4회 재다운로드 방지) · 한 IP·한 파일 한 시간 `IMG_IP_CAP_N`=60 넘으면 429. 시험 `tests/test_egress.py` 16 · 변이 13/13 · `test_stage1` T1-e·T1-d 를 새 가드에 맞춤. ★배제한 것(측정)★: 대시보드 /ws 8.4GB/월(아이온2 /diag/perf) · 업데이터 이미지 고리(아이온2 24대 로그: 하루 2장). 코드로 배제: /live(보는 사람 있을 때만) · 팜뷰 snapshot 10초 gzip · /ocr/labels(글자·커서) · 매크로 WS·POST 응답(작음). ★정확한 답은 Railway HTTP 로그 `@txBytes`(`railway logs --http --json`, 주인님 `railway login` 필요) 또는 이 계기판 배포 뒤 /diag/egress.★
- **★재개 메모 (2026-09-24, v2 반증 1부 — NO PUSH)★** v2 묶음(아래 P0·반증 C/D/v2 고침)은 아이온2 반증자가 1부를 냈고 2부(배포 순서·스테이징·이행·뮤턴트)가 온다.
  - ★v4 (2026-09-24, v3 델타 반증 — push 보류, 막던 1건)★ 고침(코드에만·미배포): #1 `main._fv_err` 에 `ok:false`·`err`(=`error`) — 배포된 팜뷰 5.54 dashCmd 는 `j.ok===false` 일 때만 실패, 문구는 `j.err` → 함대 창 400 이 «보냈습니다»+성공음이던 것(모든 `/api/fv/*` 에러가 같은 모양, 더하기만) · #2 `_kina_int` 문자열 32자 상한(5000자리 → int 4300자리 한도 500 → 400) · #3 받아 간 행이 ack 없이 치워지면(폴링 길 `supersede_updater_command`·10분 만료) 새 상태 `handed_noack`(«받아감 — ack 없음, 돌았을 수 있음», 대시보드 라벨 main.py 인라인·`static/dashboard.js` 둘 다), 늦은 ack 는 acked, `_NEWER_ALIVE` 에 포함 · #4 `fv_updcmd_list` 가 내준 행을 거절하면(`not _claimed`) `_UPDCMD_OWNER`·`_UPDCMD_FV_PC` 를 푼다(업데이터 큐 머리 10분 막힘) · X7 ★함대 창은 물리 PC(`_base_pc`)로 센다★ — A7 의 «대» 는 기계 수, 카드 셋 = PC 한 대(물리 3대·카드 8장에서 막히던 것) · 창 시계는 처음 보낸 시각(`setdefault`, 재전송이 15분을 늘리지 않음) · #6 `pc_clock` 을 `delete_pc_all_data` 가 지움(+`remove_pc`·은퇴가 `_PC_CLOCK` 캐시). 시험 새 `tests/test_refute_v3.py` 34(«v3:» 16줄은 반증자 v3 사본에서 전부 실패 확인 · X3g `_NEWER_ALIVE`·B2~B5 는 v4 독립 반증 에이전트 결과: B2 창 면제 명령의 confirm_fleet 는 «확인한 PC» 가 아님 · B3 요청 하나의 셈도 물리 PC · B4 DB 바쁨 판정에 handed_noack · B5 대리 문자 400 에 ok/err · tw_tokens 한 줄(`python tw_build.py`, CSS 변화 없음)) · 반증자 `test_brk_v3.py` 는 X5(설계)·X7(의도한 변경) 빼고 통과. 뮤턴트 `scratchpad/mut_p1.py` 에 11개 더함. 기록만: #5 PC 시계 하루 넘게 느림 → 두 번 뺌(SHARED_ISSUES 끝 절, KF1-b 가 닫는다) · #6 팜뷰 `dashapi.json` timeout 은 11초 아래면 올려 쓰라(FC1-4 ⑧, M16 은 기본값만 본다). ★stop 면제(함대 창에서 정지 쪽을 안 셈)는 주인님 정책 사안 — 아이온2 가 올림, 코드는 그대로★.
  - ★순환 전환 상한 (2026-09-24 아이온2 요청, PC-21 — 사고 522 부류)★ `main._rot_switch_age` — 전환 단계가 ROT_SWITCH_MAX(20분)를 넘으면 ① 단계 시작 5초 뒤부터 이 PC 카드에 들어온 새 `switch_launcher`/`switch_account`(사람의 한 대 재시도 포함) 시각부터 다시 잰다(명령 id 마다 한 번, `database.latest_command_after`) ② 그래도 넘었는데 카드가 acct_switching 을 5분 안에 보고 중이면 ROT_SWITCH_HARD(40분)까지 늘린다. 둘 다 없으면 예전처럼 20분 ⛔(그물 유지 — ops 게이트 38 rotttl_test 13/13 그대로). ★반증 에이전트가 재현한 것 고침★: BRK-1 진행 신선도를 매크로 시계(last_active, PC 마다 최대 301초 어긋남) 대신 ★서버가 받은 시각★ `_updated_at` 으로(`main._rot_recv_fresh`, 모르면 신선 아님) · BRK-3 `latest_command_after` 가 cancelled(매크로 거부)·expired(배달 안 됨) 명령은 안 센다 · BRK-2 새 전환 단계에 들어갈 때 `switch_ext`·`reclock_cmd` 를 지운다(두 곳) · 조회 await 사이 해제된 순환에는 «다시 잽니다» 를 안 보낸다. ★남긴 것★: 사람이 새 전환 명령을 계속 보내면 총 대기에 상한이 없다(명령마다 20분·진행이면 40분 — 사람 손으로만 늘어난다, 자동 재전송 고리 없음 확인) · `switching`(캐릭 전환)도 진행으로 친다(HARD 로 묶임). 재지시 #142-1 + ★v4 반증 1차(아이온2) F1~F4★: 사냥 확인은 `ROT_HUNT_OK`(hunting·moving·abyss)만 — `ROT_SESSION`(corridor·nightmare·dungeon·awakening)은 매크로가 세션 표시로 보고하므로 «세션 중 대기»(`main._rot_session_wait`, 말 한 번, 첫 목격부터 `ROT_SESSION_MAX` 90분 넘으면 ⛔). 시작 대기에서 세션이 끝나 idle 이면 ▶start 를 ★한 번★ 다시(오늘 끝난 카드면 전환 단계 idle 길로), 전환 대기에서 목표 카드가 세션이면 starting(세션 대기)로, 작업 순환은 세션 중엔 작업을 안 보낸다(F1·F3 — 옛 판은 사람 세션이 start 를 밀어내면 사냥 단계로 넘어가 12시간 침묵). `_rot_active` 신선도 목록에 다섯 상태(F2). 거부 판정 `main._rot_rejected` 문구 셋(«→ 'X' 거부»·«'X' 명령을 거부»·«'X'를 거부», 작업·수집 두 곳, F3). `database.latest_command_after` 문서 정정 — «이전 명령 처리 중» 거부는 acked 로 남아 못 거른다(F4). ★v4 델타 반증(아이온2 d1_sess) 고침★: 세션을 볼 때마다 단계 시계(`since`)를 다시 잡는다(`_rot_session_wait`) — 세션이 7분(작업 전환 20분)을 넘긴 뒤 captcha·reconnecting·paused 한 틱에 곧바로 ⛔ 하던 것 · 세션 대기 상태에 두 대기를 더함 `ROT_SESS_HOLD`(= 세션 넷 + nightmare_wait·awakening_wait — 전환·시작 대기에서 명령을 안 보내고 90분까지 기다림) · 세션 뒤 idle 은 ★연속 2틱★(`ROT_SESS_IDLE_N`)이어야 ▶start 재시작(한 번)을 쓴다 · ⛔ 문구는 «세션 대기가 끝나고 N분째 사냥이 안 잡힙니다(status=…)/카드 보고가 없습니다(오프라인·보고 끊김) (세션 대기 M분[, ▶시작 한 번 다시 보냄])», 작업 전환은 «세션 대기가 끝나고 N분째 작업을 못 보냈습니다». ★여쭐 것★: 시작 대기에서 `nightmare_wait` 는 작업 순환(tasking)에선 «세션이 끝난 표식 → 이 계정만 접고 다음» 인데 여기선 기다린다 — 매크로가 악몽 대기를 계속 보고하면 90분 뒤 ⛔ 다(옛 판은 7분 ⛔). 악몽 대기에 ▶start 를 보내도 되는지는 주인님 몫. → ★주인님 확인 전 임시 결정(아이온2, 2026-09-24): 악몽 대기(nightmare_wait)에는 start 를 보내지 않는다 — 90분 뒤 ⛔ 그대로(악몽 시간표를 깨는 것보다 늦게 알리는 쪽이 안전)★. 시험 `tests/test_rot_switch_clock.py` 53(D-R1·R3·R3b·R5·R6·R6b·R9·R10·R10b·T7·T9·T10 — 고치기 전 main 에서 D 전부·S-1 실패 확인 · 아이온2 하네스 S1·S1b·S2·T1·F2 장면을 옮김 + 돌연변이 생존 3개를 죽이려 T-2 작업 전환 중 세션 30분·S-1c 안내 한 번·F3-d 수집 거부 문구 · «old:» 줄은 고치기 전 main 에서 실패 — 돌연변이로 확인). 게이트로 묶는 것은 아이온2(ops) 몫.
  - 1부 고침(코드에만·미배포): A 창고키나 보낸 시각 `main._kina_sent_at` 다시 — 요청마다 직접 증거(sent_at › 비merge collected_at › 새 판독 merge 의 kina_read_at) · 배운 어긋남은 재전송에만, 최근 2h 표본 3개가 60초 안에서 맞을 때만(표 `pc_clock`, `database.pc_clock_get/put`) · 하루 넘는 증거는 버림(M3) · 창고키나 0 에선 안 배움 · 배운 값으로 버려지면 PC 시계로 재판정(`upsert_char_info(sent_learned)`) · 밀려난 옛 판독(순번·시각 둘 다 옛것) stale · `_kina_int` 엄격. B `updater_commands.handed_at` + `database.mark_updater_handed`(main 폴링이 실제로 준 뒤에만 — get_pending 안에서 적으면 U-13 경쟁에서 둘 다 못 받았다) · 내준 행은 superseded 안 됨(폴링·팜뷰 집기 둘 다) · 팜뷰는 내준 행을 안 집고 90초 안 내준 같은 종류는 busy. 그럴듯: 함대 15분 창 `_FV_FLEET_SEEN` · 팜뷰 잠금 칸 IP+토큰해시 + IP 전체 300.
  - 시험: 새 `tests/test_refute_v2p1.py` 28(v2 복사본에서 19 실패 확인 — 반증자 사본 `aff4c933…/dashv2/kina/server` 에 파일만 넣어 돌림) · 옛 규칙 시험 고침 S-5(표본 셋)·S-6b·G-1b·K-12d(+K-12e~g, test_kina_guard 44)·R-3g(`_age_row` 가 handed_at 도 늙힘).
  - 늦은 ack: `database.ack_updater_command` 가 내준 행(handed_at)은 superseded·expired 여도 acked 로 적는다(팜뷰가 더 새 것을 집으면 `updater_poll_command` 가 옛 행을 치운다 — B-4b).
  - 뮤턴트 `scratchpad/mut_p1.py` 21개 전부 죽음(처음 살아남은 5개는 A-5f·A-5g·A-Z·B-3·B-4 로 죽임). test_refute_v2p1 33. 반증 에이전트 1개 돌림(결과는 여기에 적는다).
  - 문서: FV_API 두 사본(함대 창·잠금 칸·handed) 동일 · SHARED_ISSUES KF1-b ★P0★ 로(매크로 모든 POST 에 sent_at, json 엔 저장 안 함) — 부팅 직후 재전송은 서버만으론 못 막는다.
  - ★2부 (2026-09-24, 아이온2 주 반증자)★ 고침(코드에만): #128 `[알람]` 줄은 텔레그램 전송 ★뒤★ · 실패면 « (텔레그램 실패)» · 본문 CR/LF → 빈칸(`main._alarm_event(tg_failed)`, test_alarm_event 11) · M14 진짜로 겹친 목록 시험·M16 상한 < 팜뷰 타임아웃−3초(새 `tests/test_refute_v2p2.py` 5) · M3 는 A-M3.
  - 반증 에이전트(1부 코드) 결과: ①② 내준 행이 폴링의 «팜뷰가 더 새 것» 길로 superseded·10분 expired → 늦은 ack 가 못 적음 = 이미 고친 ack(내준 행은 superseded·expired 도 acked) · ③ 함대 창이 팜뷰를 스스로 잠금 → confirm_fleet 로 보낸 PC 는 창에 안 셈(`_FV_FLEET_OK`)·창에 있는 PC 재전송 통과·정지/보기 전용 면제(`FV_FLEET_WINDOW_FREE`) · ⑥ 순번 1 + 시계 뒤로 고침 → 새 판독 전부 버림 → «방금 보낸 새 전송»(표본 셋 맞음)엔 밀려난-옛판독 규칙 안 씀(`fresh_send`, 새 전송 셋째부터 들어감, A-3f) · ④ 부팅 재전송(표본 셋 없음)은 서버만으론 못 막음 → KF1-b P0 · ⑤ 전체수집이 읽고 몇 분 뒤 찍는 kina_read_at → SHARED_ISSUES KF1-c(★KF1-b 와 같은 exe 에만★). 잠금: IP 당 추측 30→300/5분(토큰 엔트로피가 높아야 함).
  - ★(옛 목록 — 아래 «이번 push 목록» 표가 정본)★ 푸시 때 스테이징 목록(경로 지정, `git add .` 금지) updater 저장소: `server/main.py` `server/database.py` `server/HANDOFF.md` `server/tw_build.py` `server/static/dashboard.js` `server/static/tw.css` `server/static/tw_tokens.txt` `server/static/dashboard.css` `server/ocr_label.py` `server/CONTRACT_OCR.md` `server/tests/`의 수정 3(`test_integration_contracts` `test_regressions` `test_stage1`) + 새 시험 전부(`test_refute_v3`·`test_rot_switch_clock`·`test_ocr_label`·`test_abyss_kina` 포함, `git status --short --untracked-files=all server/tests`) · `FV_API.md` · `SHARED_ISSUES_대시보드.md`. farmview 저장소: `docs/FV_API.md`. ★빼는 것★: `server/static/dashboard.html`·`login.css`·`login.html`·`login.js`(9월 8~9일 옛 초안, 코드가 안 읽음).
  - ★이번 push 목록 (2026-09-24 아이온2 결정: v4 + 어비스 #104 + OCR #108 한 번에) — ★목록 밖은 안 나간다★ · 목록만 둔 판(HEAD `git archive` + 이 파일들) `verify.py` = 아래 증거★ · 스크립트 `scratchpad/pushlist.py`
    | updater 저장소 경로 | 묶음 |
    |---|---|
    | `server/main.py` | 혼합: P0·v2(09-23 P0 묶음·반증 1·2부) · v3 · v4(+순환 P0·전환 상한) · 어비스 · OCR(끝 세 줄) |
    | `server/database.py` | 혼합: P0·v2 · v3 · v4(handed_noack·`latest_command_after`) |
    | `server/HANDOFF.md` | 문서(전 묶음) |
    | `server/static/dashboard.js` | 미러(`verify.py --sync-static`): P0·v2 JS 반증 · v4 handed_noack 글자 · 어비스 카드 줄 |
    | `server/static/dashboard.css` | 미러: 어비스(전광판 10칸) |
    | `server/static/tw.css` | P0·v2 (Tailwind 미리 뽑기, 반응속도 #285) + 어비스 규칙 1개 |
    | `server/static/tw_tokens.txt` | P0·v2 + v4(`handed_noack:[`) + 어비스 |
    | `server/tw_build.py` | P0·v2 (tw.css 생성기) |
    | `server/ocr_label.py` | OCR |
    | `server/CONTRACT_OCR.md` | OCR(계약서) |
    | `server/tests/test_integration_contracts.py · test_regressions.py · test_stage1.py (수정 3)` | P0·v2 |
    | `server/tests/test_push.py · test_summary.py · test_tailwind.py · test_fvdb_b.py · test_js_b.py · test_sec_b2.py · test_state_b2.py · test_merge_b2.py · test_tgcq_b.py · test_rotation_b.py · test_breaker_v2.py · test_fv_found.py · test_fv_holes.py · test_kina_guard.py · test_kina_ledger.py · test_alarm_event.py · test_refute_v2p1.py · test_refute_v2p2.py (새 18)` | P0·v2 |
    | `server/tests/test_refute_v3.py` | v3 반증 → v4 고침 |
    | `server/tests/test_rot_switch_clock.py` | v4(순환 P0·전환 상한·반증 BRK) |
    | `server/tests/test_abyss_kina.py` | 어비스 |
    | `server/tests/test_ocr_label.py` | OCR |
    | `FV_API.md` | 문서: P0·v2 · v4(ok/err·물리 PC) · 어비스 절 · OCR D 절 머리 |
    | `SHARED_ISSUES_대시보드.md` | 문서: P0·v2 · v4(FC1-4 ⑧⑨·X5) |
    | (farmview 저장소) `docs/FV_API.md` | `updater/FV_API.md` 와 바이트 동일 사본 — 팜뷰 방 push(그 방의 다른 수정과 같은 파일이라 팜뷰 방이 정한다) |
    - ★안 나가는 것★: `server/static/dashboard.html`·`login.css`·`login.html`·`login.js`(9월 8~9일 static 3단계 초안 — 코드가 안 읽음, 주인님 결정 대기) · 저장소 루트의 아이온2 파일들(.gitignore·images2_bak·seed.*·혼종_통합.txt* 등).
    - ★static 3단계 확인★: 이번 diff 의 main.py 에 static 파일 서빙·경로 변경 코드는 없다(`StaticFiles`·`app.mount`·`/static` 0건). static/ 에서 읽는 것은 `TW_CSS_PATH`(tw.css 를 HTML `<head>` 에 인라인, 없으면 예전 Play CDN) 하나뿐 — 이건 3단계(화면을 static 으로 서빙)가 아니라 v2 반응속도(#285) 몫.
  - ★재지시 #142 «적용 안 된 거 싹 다 다시» (2026-09-24, `web/.claude/ops/미적용_재지시_20260924.md` §2) — 분류★
    ★#152(2026-09-24 아이온2): 주인님 5시간 사용량 50% — v4 = 순환 P0(1번) + 반증 지적만. 2·3·4 는 다음으로 내렸다 → ★그 뒤 아이온2 결정: 2·4 는 v4 와 같이 나간다(아래 표)★.★ ★단 2·4 는 이미 main.py 작업 트리에 들어 있다(같은 파일이라 경로 스테이징으로 못 가른다) — v4 를 푸시하려면 2·4 도 같이 나가거나, 되돌리려면 `scratchpad/pre_abyss_bak/`(어비스 전 main·static) + main 끝 OCR 세 줄 삭제 + `ocr_label.py`·시험 2개 빼기. 결정은 아이온2.★
    | # | 항목 | 분류 | 상태 · 이유 |
    |---|---|---|---|
    | 1 | P0 어비스·회랑·악몽 중 «사냥 진입 실패» 오탐 | **v4 포함** | 어비스=사냥 확인, 회랑·악몽·던전·각성전=세션 대기(v4 반증 F1~F3) · 시험 H·S·T·F |
    | 2 | P0 어비스 수익 전광판·시간당·100만↓ 빨강·옮기면 재측정 | **v4 와 같이**(아이온2 결정, 9f27627 배포) · ★주인님 #159(2026-09-24 배포 뒤) 고침★: 함대 1.1.1003 은 숫자 칸(abyss_kina_state…)이 없어 카드가 옛 글자 줄을 그대로 찍었다 → 빨강 없음·PC-17b «+-6,349,199 · 시간당 -12,622,269» 음수 노출. `abyssCardLine` 옛 글자 경로: 음수가 하나라도 있으면 회색 «판독 오류 — 재측정 대기»(숫자 안 냄) · 「+X 키나 · 시간당 Y (HH:MM~/부터)」 는 KST 지금−HH:MM 으로 잰 분 < min_mins 면 중립, 아니면 Y < red_rate 빨강 · 못 읽는 모양은 예전 그대로. 서버 `_abyss_parse_legacy` 는 음수 글자를 못 읽어(None) 오늘 합계·시간당 합에 안 섞인다(L-6~8). 시험 L-1~8(127, L-1~5 는 두 시간대 · 9f27627 main 에서 전부 실패). ★운영 대조 뒤(아이온2 05:3x, rate_n 19 = 양수 카드 17 + 음수 2)★: 음수 글자는 버리기만 해서 기록(sig_at)이 그대로였고, 전광판이 그 PC 의 ★마지막 좋은 시간당★ 을 ABYSS_LIVE_S(300초) 동안 합·대수에 넣었다(좋은 판·음수 판이 번갈면 계속) → `_abyss_ingest` 가 음수 옛 글자에 `unread_at` 을 찍고, `_abyss_billboard` 는 unread_at > sig_at 이면 시간당 대신 wait_n 으로 센다(오늘 합계는 그대로). 시험 L-9~12(131, L-10 은 5b0d99a main 에서 실패). ★v4 반증 3차(아이온2) 고침★: 깨진 저장본(글자·NaN·목록 줄)이면 `_abyss_billboard` 의 `int()` 가 터져 /summary·/api/fv/snapshot 이 전 PC 500 → 칸마다 `main._abyss_i`(NaN·inf·글자 = 0, `math` 안 들임) + dict 아닌 줄 건너뜀 + `_fv_build_snapshot` 에서 전광판 try(실패면 `today:null`·`error:true`, 나머지 스냅샷은 그대로) · 시험 V3-a~c(114 — `_abyss_restore` 가 저장본 칸을 이미 0 으로 고쳐 넣어 가드가 시험에 안 닿던 것을 돌연변이로 찾아, 복원을 안 거친 깨진 메모리 줄로 직접 찌른다). ★낮음 — 기록만, 다음 묶음★: ① 배포·날 바뀐 직후 ~6분 가짜 빨강(표본이 쌓이기 전 시간당이 낮게 나옴) ② 시계가 뒤로 가면 그 구간이 사라짐 ③ state ok 인데 rate null 이면 빨강 ④ 1e300 같은 값이 카드에 그대로(표시만) | `scratchpad/abyss2/PATCH_v2.diff` 를 지금 main 에 이식 — ★23덩이 중 10덩이는 어비스가 아니라 그 사이 들어온 updcmd·`maint_task`·`fv_unknown`·팜뷰 ack 를 ★되돌리는★ 줄이라 버렸다(퍼지로 «성공» 하던 #11·#22 포함)★, 어비스 11덩이 + 손으로 합친 2덩이. 팜뷰 PC 칸 `progress.abyss_kina_*` 더함(FV_API «2026-09-24 추가 — 어비스 수익» 두 사본) · 오늘 누적 안 줄어듦(S2-a) · since 바뀌면 빨강 해제(S2-b). `tests/test_abyss_kina.py` 111. 배포는 매크로 1.1.1004 와 같은 날 |
    | 3 | P0 push_state 전후 ms (장부 #114) | 다음(측정 중단 — #152. 도구 `scratchpad/push_ms/` 반쯤) | 코드는 이미 작업 트리(HEAD 23 → 39곳, `tests/test_push.py`). ms 는 HEAD 사본 vs 작업 트리를 로컬에서 같은 조건으로 잼 — 운영 실측은 배포 뒤 |
    | 4 | P1 OCR 라벨 `/ocr/*`·`/api/fv/ocr/*` (#108·#125) | **v4 와 같이**(아이온2 결정) · ★v4 반증 2차(아이온2) 중간 고침★: 라벨 된 묶음은 비우기 1단계가 안 지우고 3단계는 파일만 지워 비슷한(near) 이미지마다 `ocr_img` 줄이 끝없이 쌓였다 → 묶음당 줄 상한 `ocr_label.OCR_CLUSTER_ROWS_MAX`(64) — 꽉 차면 새 줄·파일 없이 가장 가까운 구성원의 count 만 올리고 응답에 `capped:true`(label_match 는 near) · 시험 V2-a~d(near 200장 → 줄 64·파일 64·count 201) · ★v4 델타 반증(아이온2 d3_ocr) 고침★: 상한 검사를 ⑥ 자리 만들기(`evict_for`)보다 앞으로 — 파일을 안 쓰는 capped 제출이 비우기를 돌려 남의 대기 묶음을 통째로 지우던 것. 자리를 만든 뒤 묶음은 다시 찾는다(비우기가 찾아 둔 묶음을 지웠을 수 있다) · 시험 V2-e~h(268, V2-f·g 는 고치기 전 판에서 실패 · V2-h 는 대조: 파일을 쓰는 새 묶음은 여전히 비운다). 남은 틈: 다시 찾은 묶음이 마침 꽉 차 있으면 한 줄 넘칠 수 있다(비우기가 가장 가까운 묶음을 지운 드문 경우). 낮음(기록만): capped 로 센 이미지의 dhash 는 near 힌트에 안 나간다(줄이 없으니) · 전광판 `error:true` 일 때 카드 툴팁에 «KST null» 이 보인다(day 가 null). ★낮음 — 기록만, 다음 묶음★: ① 전역 상한을 테넌트가 같이 써서 한 테넌트가 채우면 다른 테넌트가 507 ② 429 판정이 본문을 다 읽은 뒤에야 난다 ③ `/ocr` 페이지에 X-Frame-Options 없음 | `server/ocr_label.py` + main 끝 연결 세 줄 + `tests/test_ocr_label.py` 264/264 + `server/CONTRACT_OCR.md`. FV 에러 모양을 v4 `_fv_err`(ok·err)에 맞춤. 매크로 훅과 같은 날 배포. 팜뷰 탭은 여쭐 것 8 |
    | 5 | P1 렌탈 코드 전부 폐기 | 다음 | `KILLED_TENANTS` 사용처 전수 + 56곳 삭제는 v4 와 섞으면 반증 범위가 커진다 — v4 배포 뒤 단독 묶음 |
    | 6 | P1 `rot_allow` 비었거나 `*` 아님 → 상단 빨간 경고 | 다음 | 경고만 먼저 가능. 기본값 `*` 는 여쭐 것 5 답 뒤 |
    | 7 | P1 updcmd idle PC 왕복 실측 | 할 수 없음(이 방 혼자) | 운영 PC 에 상태 바꾸는 명령 = 배포 뒤 + 아이온2 가 한 대 지목(A7) + 팜뷰 방 동시. 이 방은 코드·로컬 시험까지 |
    | 8 | P2 안 쓰는 계정 카드 «다른 계정» → «대기» | 다음 | 라벨만(오프라인 안 보이는 동작 그대로) |
    | 9 | P2 겹치는 상태 색 나누기 | 다음 | 색 표 안(초록·회색 유지)을 먼저 올린다 |
    | 10 | P2 스샷 버튼 이름 옆으로 | 다음 | 이름 안 자르고 되는지 1280·폰 스샷으로 판단 |
    | 11 | P2 1280·폰 폭 가로 넘침 | 다음 | 로컬 서버 + 브라우저로 찍어 확인 |
    | 12 | P2 구독 안 한 아이디 목록 다시 뽑기 | 할 수 없음(이 방 혼자) | 운영 데이터는 인증이 필요하고 이 방은 비번·로그인 금지 — 아이온2 가 뽑거나 읽기 전용 경로를 지정 |
    | — | static 3단계 · `/brief` · 악몽 +2 | 결정 뒤 | 손대지 않음(여쭐 것 7·10·2) |
  - 결정(아이온2 2026-09-24): 업데이터가 받은 #1 의 ack 가 사라진 사이 같은 종류 #2 를 누르면 ★둘 다 차례로 돈다(R-3d 유지)★ — 사람이 두 번 누른 것은 버리지 않는다(사고 523·535 부류). KF1-b·KF1-c 는 아이온2 가 프로그램방에 넘김(같은 exe 조건 포함). 지금: 아이온2 델타 반증 대기 → 그 뒤 푸시(«go» 에만).
- **P0 묶음 = 코드에만(미배포). 아이온2 반증 3차(업데이트 «한 길») 고침 → 아이온2 재확인 + 푸시 승인 대기.** 내용: 창고키나 장부(`database._kina_ledger_replay`·`kina_read`·부팅 복구 `heal_reverted_kina`, 시험 `test_kina_ledger` 36) · 업데이트/재시작 «한 길»(팜뷰 id 표 `updater_fv_claim`, 안-감 확실할 때만 되돌림 `_fv_not_reached`, ★ack 없는 팜뷰 명령은 업데이터에 안 넘기고 `fv_unknown`★, 재배포 뒤 목록 복구 `_fv_rebuild_from_db`, 업데이터가 90초 안에 받은 같은 종류는 업데이터 몫 `_UPDCMD_UP_PC`, 집은 뒤 DB 재확인 `updater_command_status`, 같은 종류만 덮기 `_older_same_kind`, 시험 `test_fv_found` 84·`test_fvdb_b` 49) · 명령 표 정리를 부팅에서 백그라운드로(`maintain_command_tables`·`_cmd_table_maint`, 1M 행 부팅 0.01초) · FV 명령 8초 상한 `err` · 대시보드 «업데이터 명령» 내역(`renderUpdHistory`).
- 팜뷰 쪽 남은 것 = `SHARED_ISSUES_대시보드.md` FC1-4(UPD_DONE 영속·ack 재시도·reached).
- **FV 스냅샷 조립 실패 때 마지막 성공본 (2026-09-24 아이온2 승인, 89efe25 묶음과 따로 커밋, 코드에 넣음·미배포)**: `fv_snapshot` 이 조립 예외에 마지막 성공본(`_fv_snap`)이 `FV_SNAP_STALE_MAX`(120초) 안이면 200 `cached:true` + 진짜 `cache_age_s`, 넘으면 예전대로 500. ★따로 저장소를 안 둔다★ — 3초 캐시 본문을 그대로 쓰므로 판매 차감·장부 재적용의 무효화(`_fv_snap["body"]=None`)가 폴백도 같이 지운다(바뀐 걸 아는 옛 값은 안 낸다). raw 거름(B-FV1) 그대로. 시험 새 `tests/test_fv_snap_fallback.py` SF-0~5, 변이 4/4(폴백 없음·상한 없음·cached:false·raw 안 거름). FV_API 두 사본 «캐시»·오류표. 500 코드는 안 바꿨다(계약 그대로). ★아이온2 반증 반영(같은 커밋)★ ① 커밋 뒤 503 → 재전송 dup: 캐시를 비우고 그 tid 로그줄이 없으면 장부 줄(`database.kina_adjust_row`)에서 «— 재전송 때 남김» 으로 되살림(`database.log_has`, K201-e~g) ② ★세대★ `database.char_info_gen()` — char_info·kina_adjust 쓰기 넷(upsert_char_info·adjust_char_kina·heal_reverted_kina·delete_pc_all_data)이 커밋 직후(연결 안) 올리고, `fv_snapshot` 은 조립 전 세대를 적어 다르면 3초 캐시도 폴백도 안 준다(판매·새 판독·조립 중 쓰기, SF-6~9) · 쓰기 함수 전수 AST 시험 SF-10 ③ 되돌림 복구 로그줄 실패도 200(M10 → K201-h) ④ 503 retry 는 `sqlite3.Error·OSError·asyncio.TimeoutError` 만, 코드 버그는 500(K201-i). 변이 12/12. ★반증 2차(같은 커밋)★ SF-10 을 AST 로 — `await …commit()` ★바로 다음 문장★이 `_char_info_bump()` 호출이어야(주석만·커밋 앞·글자만 잡음, 자가시험 SF-10s) · `main._worker_warning` — WEB_CONCURRENCY·UVICORN_WORKERS·GUNICORN_WORKERS·`--workers` 가 2 이상이면 lifespan 이 ★세 줄 크게★ 경고(캐시·세대·WS·순환이 프로세스 안 상태, uvicorn 은 $WEB_CONCURRENCY 를 기본 워커 수로 읽는다; SF-11) · 폴백 나이는 실패한 뒤 잰다(SF-12) · dup 로그줄 바늘은 판매 줄에만 있는 «원, tid X) »(부팅 복구 «(tid X)» 가 가리지 않게, K201-j). ★내가 낸 사고★: `_worker_warning` 을 `@asynccontextmanager` 와 `lifespan` 사이에 끼워 장식자를 빼앗았다(서버가 못 떴을 것) — SF-11 이 잡았고 SF-11c 가 지킨다. 변이 6/6.
- **팜뷰 #201 r3d 계약 둘 (2026-09-24 아이온2, 코드에 넣음·미배포 — 팜뷰 #201 과 같이 나간다)**: ① FV `progress.kina_read_age_s` — 서버가 ★새 판독으로 받아들인★ 마지막 `kina_read.read_srv`(창고를 읽은 순간, 서버 시계) 기준 정수 초, 모름 10^9. `get_all_char_info` 가 `LEFT JOIN kina_read` 로 같이 읽고 `_attach_char_info` → `_kina_read_at` → `_fv_pc_view`. merge 판독(사냥 끝 창고, 사고 569 — collected_at 을 안 바꾼다)에도 바뀐다. ★재전송·버려진 옛 판독·판독 시각 없는 보고는 안 바꾼다★ — 재전송된 옛 값을 «방금 읽음» 으로 보이면 팜뷰가 반영 안 된 판매를 건너뛰어 창고를 부풀린다(그래서 새 칸·수신 시각 폴백을 안 만들었다; KF1-c 전 매크로 카드는 10^9). 시험 KR-1~7 + test_contracts. ② `POST /api/fv/notify {key, text, pc_id?}` — 팜뷰 → 주인님 텔레그램(기존 `tg_send_text`). 같은 key 한 번만(`fv_notify` 표, sent 는 끈적 — 재배포에도) · 텔레그램까지 간 시도 1분 3·1시간 20 → 429 `retry_after_s` · 같은 key 동시 → 409 busy · 전송 실패 502 retry(key 안 먹음) · 장부 DB 오류 503 retry(안 보냄) · 텔레그램 미설정 503 retry:false · 감사 = `fv_notify` 표(`GET /api/fv/notify`) + pc_id 카드 로그 «[팜뷰 알림] …». 음소거는 안 본다(카드 알림이 아님). 30일 보관. 시험 `tests/test_fv_r3d.py` N-1~19(36/36). FV_API 두 사본 새 절. 변이 21/21. ★내 반증 에이전트★: 장부 쓰기 실패 뒤 dup 요청이 그 key 를 «dup» 로 적어 재시작 뒤 다시 보냈다 → 기억(_FV_NOTIFY_SENT)으로 dup 이면 «sent» 로 고쳐 적음(N-15b). 기록만: 판독 시각 없는 옛 매크로의 전체수집은 kina_age_s 만 바꾼다(팜뷰는 두 나이 중 작은 쪽을 쓴다 — FV_API 에 적음) · 옛 매크로 merge 판독은 어느 칸도 안 바꾼다(못 막음: 읽은 때를 모른다) · sent_at 없는 merge 판독은 기존 `_kina_sent_at` 이 kina_read_at 을 보낸 때로 써 «방금» 이 된다(지금 매크로는 sent_at 을 늘 보냄) · 텔레그램 10초 시간초과 후 실제 도착이면 재전송이 두 번 보낼 수 있다(적어도 한 번 — FV_API 에 적음). ★아이온2 58715f9 반증(뒤 커밋)★ (a) text 1000자 자름(N-20)·PC 시계 어긋남에도 read_srv 기준(KR-8·8b) (b) 시간 상한은 ★실제로 보낸★ 것만(실패는 분 상한만) + sold_fail: key 몫 5칸(다른 key 는 15) — 팜뷰 버그가 딴 key 를 쏟아도 판매 실패 알림은 나간다(N-10d~f) (c) 보낸 key 의 dup 은 기억(_FV_NOTIFY_SENT)으로 DB 없이 답한다 — 장부 쓰기 실패 key 만 다음 dup 때 sent 로 다시 적어 본다(N-4b·4c·15b).
- **p6 수집 중 판독 두 번 빼기 (2026-09-24 SHARED_ISSUES_팜뷰 r3e, 코드에 넣음·미배포)**: `database._kina_after_read` 는 판독 시각 뒤에 ★기록된★ 차감을 새 판독 값에서 다시 뺐다 — 기록 시각은 게임 안 인계가 아니라 팜뷰가 적은 때라, 인계 → 판독(판매 반영) → 차감 기록 → 판독 도착 순서에서 두 번 뺐다. `kina_adjust` 에 `why.hand_at`(인계 epoch 초, 선택) — 판독 시각 이하(같은 초 포함)면 다시 안 뺀다. 모르면 예전대로 뺀다(`KINA_UNKNOWN_HAND_DEDUCT = True` — 반증 ③(판매 전 판독이 늦게 와 판매를 되살림)을 다시 열지 않으려고; ★«모르면 덜 뺀다» 로 가려면 False 한 줄, 아이온2 결정★). 받는 곳에서 모양 검사 400(글자·bool·밀리초·음수·하루 넘는 미래). 인계를 기록 시각으로 자르는 코드는 넣었다가 뺐다 — 부르는 쪽이 at > read_at 행만 보므로 효과가 0(변이가 살아남아 알았다). 시험 P6-0~h(옛 코드면 P6-a 가 400 으로 실패). ★팜뷰가 hand_at 을 보내야 닫힌다★.
- **#208 표층 status `surface` (2026-09-25 주인님 «표층 중인데 대기», 코드에 넣음·미배포)**: 매크로가 표층 세션 중 `surface` 를 보낸다(회랑과 같은 방식, 프로그램 방). 카드 `STATUS_CFG.surface` «표층 진입중»(회랑 모양) · `DK_BLEED` · `AUTO_IDLE_BUSY` · `CMD_TRACK` start/surface exp · 순환 `ROT_SESSION`(→ `ROT_HUNT_LIKE`·`ROT_SESS_HOLD` 따라옴) · `ROT_ST_KOR`. `ROT_TASK_BUSY_ST` 는 작업표라 안 넣음(표층은 순환 작업이 아니다). 같이: `_fv_parse_cmd_table` 이 줄 끝 `//` 주석 달린 줄을 버려 `surface` 명령이 FV 명령표에 없던 것. 시험 `tests/test_surface_208.py` 15(옛 코드면 14 실패) · 변이 10/10. 팜뷰 `fvdash.ACT_ST` 에 surface 는 팜뷰 몫.
- **합동 반증 후속 (2026-09-25, 코드에 넣음·미배포)**: ① I8 — 대시보드 `ttsText` 에 팜뷰 70e3dbd 의 전각 숫자 줄(`[０-９]`→ASCII)을 글자 그대로 옮기고 `_TTS_FW_PENDING` 을 비웠다(이제 전각도 묶임). 시험은 `FV_I8_REF=<팜뷰 커밋>` 으로 팜뷰 끝 커밋과 묶을 수 있다(기본 HEAD — ★개발컴 팜뷰 main 이 fv197 보다 낡으면 I8 빨간불★). ② D11 — `_fv_notify_limit` 대기 초가 [len−cap] 기준인지 N-21(변이 [0] 죽음). ③ FV_API `hand_at` = 인계 시각의 하한.
- **#218 OCR 맵 이름 자동 닫기 (2026-09-25 주인님 «홍옥의 섬 계속 올라오네», 코드에 넣음·미배포)**: `ocr_label.OCR_AUTO_SITES`(analytics_py__map_loop → 16개 이름) · `auto_label`(묶음 ★모든★ 답이 strip 뒤 같은 한 이름) · `_auto_eval`(제출 때 — 대기→auto, 다른 답 붙으면 auto→대기, ocr_hist 있는 묶음은 안 건드림) · `auto_sweep`(대기 중인 것, `_ensure_tables_once` 에서 프로세스마다 한 번 · dry 지원). status `auto` 는 대기열·기록·/ocr/labels 에 안 나가고 stats `auto` 로만 센다. 비우기 2층(나쁨과 같이 파일만). 시험 `tests/test_ocr_auto_218.py` 22. 실서버 대기(GET 만) 드라이런: map_loop 대기 15 중 13 닫힘(홍옥의 섬 7), 2 는 제미나이 오타(엘뒨강·요세)라 남음.
- **#228 OCR 믿을 만한 자리 자동 닫기 (2026-09-25 주인님 «OCR 80개 찼다», 코드에 넣음·미배포)**: `ocr_label.site_trusted`(사람 라벨 ≥ `OCR_TRUST_MIN_LABELED` 200 · 최근 `OCR_TRUST_WINDOW` 200 장 불일치 0 — 순서는 MAX(라벨 시각, 이미지 도착): 옛 라벨 묶음에 오늘 붙은 오독도 창에 든다 · 나쁨 묶음의 답 있는 이미지는 불일치) · `_trust_spot`(ocr_meta `trust_n:<tenant>:<site>` 셈, `OCR_TRUST_SPOT_EVERY` 20 번째는 대기) · `_trust_eval`(새 묶음만 auto — 근접 합류로 다른 답이 붙으면 대기로). 끄는 상태는 따로 없다 — 표본의 다른 답이 최근 창에 들어가 저절로 꺼지고, 창이 깨끗해지면 켜진다. 맵 이름 site 는 #218 규칙만. 시험 `tests/test_ocr_trust_228.py` 23. 기존 대기 묶음은 안 쓸어낸다(새 묶음만).
- **pin_reset 버튼 (2026-09-26 PIN 3차, 코드에 넣음·미배포 — 매크로 lc 77026c4 와 같이/먼저 나간다)**: 카드 메뉴 «PIN 막힘 풀기 (그 계정만)» 칸 · `refreshPinButtons`·`pinResetFromMenu` → `sendCmd(id,'pin_reset',{label: ACCT_LABELS[n-1]})`. 서버 `/command` 는 허용 목록이 없어 그대로 큐에 넣는다(접미사 pc_id 유지). ★CMD_SILENT★(칩을 띄우면 물리 PC 한 칸 pendingCmds 에서 막힌 전환의 칩을 덮는다) · ★팜뷰 명령표에서 뺌 `_FV_EXCLUDED_CMDS`★(명령표가 CMD_TRACK/CMD_SILENT 파싱이라 화면에 넣으면 팜뷰 7대·함대로 열렸다 — 한 PC 명령). ★버전 문(아이온2 HIGH)★ — 함대 1.1.1012 엔 처리기가 없어 사람 명령이 도는 전환을 끊고 모르는 명령 알람을 낸다: 화면은 카드 macro_version < `PIN_RESET_MIN_VER`(JS 한 줄이 정본, 서버 `_PIN_RESET_MIN_VER` 가 파싱 — ★릴리스 때 아이온2 가 확정★)이면 버튼 대신 잠긴 안내, 서버 `_pin_reset_ok` — 그 물리 PC 의 가장 최근 보고 ★와★ 받는 카드 자신의 보고가 둘 다 문턱 이상이어야(미만·모름·문턱 못 읽음이면 409, 숫자 비교). ★배달 때 다시 본다★ `_drop_stale_pin_reset` — 폴링·WS 재접속 배달에서 못 넘으면 cancelled 로 내리고 안 보낸다(넣은 뒤 롤백·재시작 대비). 팜뷰 명령표가 비면 503(닫힌 채로 실패). 시험 `tests/test_pin_reset.py` 43(node 로 화면 코드 실행).
- **#201 부류 읽기 점검 — 매니아 경로 (2026-09-24 아이온2, 코드에 넣음·미배포)**: 대시보드의 매니아 길 넷(`/api/fv/kina_adjust` · FV 스냅샷 창고키나 · 매니아가 읽는 `/status` · 매크로 `/char_info`)을 «일시 실패 → 확정 실패·사람 호출» 로 훑음. 고침 ① `fv_kina_adjust` 가 DB 예외(바쁨 5초 초과 등)에 맨 500 → 503 `{ok:false, err, retry:true}` «같은 tid 로 다시» (커밋 뒤 연결 닫기 예외도 있을 수 있어 «안 뺐다» 고 단정 안 함) ② ★커밋 뒤★ 로그줄 실패가 뺀 판매를 500 으로 답하던 것 → 200 ok, 스냅샷 캐시는 로그줄 전에 비움. 시험 test_kina_ledger K201-a~d, 변이 3/3. FV_API 두 사본 오류표·kina_adjust 절에 503. 팜뷰 쪽(대시보드 200 ok 아니면 «차감 대기·기록만 남김» 빨간 칩+오류음, `SALE.tried` 라 자동 재시도 없음 — ui/index.html saleClose)은 팜뷰방 몫이라 보고만. 문제 없음: `/status` 는 예외면 500(빈 목록을 안 준다), 매니아 `parts/fleet.status` 가 4번 다시 묻고 `not _total_kina` 는 건너뛴다(0 으로 안 셈). 남긴 제안: 스냅샷 조립 실패(500) 때 3초 캐시보다 조금 낡은 마지막 성공본을 `cached:true`+`cache_age_s` 로 줄 수 있다 — 아이온2 판단.
- **f868aa6 반증 낮은 메모 (2026-09-24 아이온2, 코드에 넣음·미배포)**: ① FV8 가장자리 — 못 읽은 카드(저장 0)에 판매가 적히면 before=0 행이 생겨 0 이 «앎» 이 됐다 → kina_ledger 는 `before > 0` 행만 센다(`database.get_all_char_info`, test_kina_ledger L-41, 변이 죽음). ② ROT1-b 의 `(_cpc or not active)` 에서 죽은 `not active` 를 뺐다(바로 위 `if not active: return`, B-ROT1-f 변이 여전히 죽음). ③ 커밋 본문 줄 번호는 커밋 전 것 — 그대로 둔다(아이온2).
- **#114 재반증 (2026-09-24 아이온2 지시, 코드에 넣음·미배포)** — `scratchpad/b/findings_r1.md` 62건을 다시 훑음. 새로 고친 여섯(전부 고치기 전 main 에서 빨간불 = 변이로 확인, 15/15 죽음): **TG5** 은퇴 id 의 `/telegram/send·photo` — 보통 알림은 음소거와 같은 응답+`retired:true`(새 reason 글자 금지 — 옛·새 매크로가 폴백한다), 강제·캡차는 중계, 카드·이벤트 행 없음(`main._tg_retired_skip`, test_tg_hard R-1~R-4) · **FV8** 창고키나 0 은 장부(kina_adjust) 행이 있을 때만 «앎»(`database.get_all_char_info` kina_ledger + 인덱스 `idx_kina_adjust_pc`, `main._attach_char_info`, test_kina_ledger L-37~L-40) · **ROT1-b** 수집 증거는 collect_pc 카드 값만(형제 active 의 옛 값으로 전환 금지, test_rotation_b B-ROT1-f) · **ROT7-b** 파섹 주소록 await 뒤·switch_launcher 직전 `_alive()` 두 자리 × 수집·작업 전환(B-ROT7-b 4갈래 + 대조 b0) · **DB8** 로그 정리 카운터가 재시작마다 0 → 처음 보는 pc_id 는 첫 줄에 정리(test_regressions ②-c·②-d) · **CQ9** 대기 명령 칩·막대의 경과 초가 카드 비교를 매초 깨 대기 카드를 렌더마다 갈아 끼움 → `data-pt`(=`pendKey`) + `_rkNorm` 이 글자만 뺌(test_js_b B-CQ9-a~e). 남긴 것(SHARED_ISSUES 2026-09-24 #114 절): JS1 잔여(슬롯별 read_at, lc 짝) · ROT5(주인님) · JS8 가장자리(기록만) · FV_API total_kina 0/null 문구 제안. 곁가지: `_key_probe_blocked` 에 now 인자가 생겨 test_tg_hard H-4f 의 가짜(lambda ip)가 깨져 `lambda ip, now=None` 로.
- **cbdf3fc 반증 후속 (2026-09-24 아이온2, 코드에 넣음·미배포)**: ① `_key_probe_blocked(ip, now=None)` — check_api_key 가 자기 `now` 를 넘겨 잠금 판정과 창 초기화가 ★같은 순간★(K-5). ② verify 3-e 가 닫는 태그 `</SCRIPT >`(대소문자·공백)·`on*=` 이벤트 속성·`href/src/action="javascript:…"` 도 본다(3-e0 자가시험 4건). ③ ★`static/dashboard.html`·`login.html`(+`login.css`·`login.js`)은 추적하지 않는다★ — 서버에 static 경로가 없어 아무도 안 내보내고(main.py 에 StaticFiles·/static 없음), 만드는 도구도 부르는 코드도 없다(2026-09-08~09 분리 시도의 찌꺼기, 정본 HTML_DASHBOARD 보다 2주 낡음). 추적하면 손 복사 미러가 하나 더 생긴다(루트 CLAUDE.md §5 «손 복사 미러는 두지 않는다»). ★아이온2 결정: 지우지 않고 저장소 밖으로 옮겼다(되돌릴 수 있게)★ → `src/_bundles/dashboard_static_split_20260908/`(README.txt 에 이유·날짜). static/ 에 남은 건 dashboard.js·dashboard.css(미러)·tw.css·tw_tokens.txt 뿐 — 3-e 의 .html 검사는 앞으로 생길 HTML 을 위해 그대로 둔다.
- **c32d3eb 반증 후속 — K-4 AST · 3-e static HTML (2026-09-24 아이온2, 코드에 넣음·미배포)**: ① K-4 가 글자 검사라 `KEY_TO_TENANT.get(k)`·`for k in KEY_TO_TENANT`·주석 속 KEY_MAX_FAILS 로 빠져나갔다 → AST: 서버 .py 어디서든 KEY_TO_TENANT(또는 `main.KEY_TO_TENANT`)를 ★읽는★ 자리는 그 앞에서 `_key_probe_blocked(...)` ★호출 노드★가 있어야 한다(예외 `_init_tenants` 하나 — 설정으로 표를 만든다). 모듈 맨바닥 읽기·재바인딩도 빨간불. K-4s 스캐너 자가시험(우회 7가지). check_api_key 의 인라인 잠금 조건을 `_key_probe_blocked(ip)` 호출로 바꿈(같은 조건 — 판정 한 곳). ② verify 3-e 가 `static/*.html` 인라인 `<script>` 도 본다(옛 판은 .js 만 — 변이로 옛 판 0건/새 판 1건 확인). ★알려진 동작 변화(아이온2 수용)★: 잠긴 IP 는 차단 테넌트의 ★맞는★ 키에도 `/telegram/status` 403 → 매크로가 중계를 끄고, 정지 안내가 잠금 창(KEY_WINDOW 300초)만큼 ★최대 5분 늦게★ 나갈 수 있다. 오라클을 닫는 값으로 받아들였다.
- **`/telegram/status` 키 추측 오라클 (2026-09-24 아이온2 반증, 코드에 넣음·미배포)**: 차단 테넌트 «중계 가능» 갈래가 키를 따로 훑으며 probe 잠금을 안 봐서 잠긴 IP 가 200/403 으로 차단 테넌트 키를 끝없이 맞혀 볼 수 있었다 → `_tg_blocked_tenant` 한 곳(send·photo 와 같음). 틀린 키는 check_api_key 가 센다. 시험 `tests/test_key_probe.py` 5 — K-4 는 ★같은 부류 전수★: main.py 에서 `KEY_TO_TENANT` 를 훑는 함수 5곳(check_api_key·license_check·macro_websocket·_tg_blocked_tenant·updater_check)이 전부 probe 잠금을 본다. 변이 4/4.
- **커밋 B — Safari 뒤보기 금지 · 전각 숫자 짝 · 번거/번쩍 (2026-09-24 아이온2 반증, 팜뷰 최종 8240d6a = 411a24c 에 옮긴 ttsText 와 같은 글자)**: ① `verify.py` 3-e — 브라우저로 나가는 `<script>`(main.py·ocr_label.py) + `static/*.js` 에 정규식 뒤보기 `(?<=`·`(?<!` 가 있으면 빨간불(Safari 16.4 미만은 스크립트 전체를 문법 오류로 버린다 — node --check 는 못 잡는다). 3-e0 는 탐지기 자가시험. 변이 2/2(대시보드 JS·static js). ② I8 키 38개 — 번거/번쩍(«번» 뒤 글자) 7개 묶음. ★전각 숫자 3개(２번·PC-０９·3２번)는 파이썬 speak_text 는 읽고 팜뷰 JS ttsText 는 그대로 둔다 — 고칠 곳은 팜뷰 원본(대시보드는 글자 그대로 옮기는 쪽)★ → `_TTS_FW_PENDING` 으로 ★보이게★ 찍고(«전각 숫자 짝 미해결») 나머지는 묶음. 팜뷰가 맞추면 비운다.
- **TG7 클라이언트 반 짝 — 차단 응답에 reason (2026-09-24 아이온2, lc 2a448ed·e32c01f 짝, 코드에 넣음·미배포)**: `/telegram/send` 차단 403(정지 안내만)과 `/telegram/photo` 차단 403 이 `{detail: 예전 한국어 문구 그대로, reason:"blocked"}`, 429(정지 안내 상한)는 `reason:"blocked_cap"`. 글자 셋(muted·blocked·blocked_cap)은 `main._TG_REASON_*` 한 곳 + `tests/test_contracts.py` t_tg_reasons 가 값을 못 박는다(변이 7/7). 사진은 예전 detail 없는 `Forbidden`(키 오류·probe 잠금과 구별 불가 → 매크로가 /telegram/status 를 다시 물음) → 텍스트와 같은 문구. 차단 판정·응답은 `_tg_blocked_tenant`·`_tg_blocked_resp` 한 곳(두 창구). 미등록 키·probe 잠금 IP 는 그대로 reason 없는 403(키 추측 오라클 방지). 계약 §7 + C4-b 문구 정본. 시험 `tests/test_tg_hard.py` 20(H-4~4g·6b) · 변이 7/7.
- **4c98f2a 반증 후속 — DB10 우회 · 팜뷰 361d083 목소리 · 별칭 21쌍 (2026-09-24 아이온2, 코드에 넣음·미배포)**: ① `/setting/{key}` 가 날 키만 대조해 `retired%23pcs`·`tg%40offset`·`rot%2Callow` 가 소독(clean_pc_id) 뒤 그 키를 덮었다 → `ns("main", key) in _SERVER_MANAGED_NS` 로 저장 키 대조(시험 S-5·5b — # @ , : <, 변이 1/1). ② 대시보드 `ttsSino`·`ttsText` 를 팜뷰 ★커밋★ 361d083(#172 곁가지 2차) 에서 글자 그대로 다시 옮김 · I8 은 팜뷰 ★커밋된★ ui/index.html·alarmvoice.py(git show HEAD)와 대조 — 작업본(미커밋)에 기대면 깨끗한 체크아웃과 갈린다. ③ `PROMPT_SHA1_OLD_NEW` 21쌍(awakening.py:294 반증 2차 `ba59cd48acd3 → 5141ebd09687`, SHARED_ISSUES_아이온2 표 대조 A-1). ★Safari <16.4 뒤보기 금지(verify 3-e)·전각 숫자는 다음 커밋★ — 팜뷰 최종 ttsText(뒤보기 없는 판)를 글자 그대로 옮길 때 같이(자체 정규식 판은 `scratchpad/parkB` 에 보관).
- **TG7 후속 — 음소거 뚫기 한 곳 `_tg_hard` (2026-09-24 주인님 결정 via 아이온2, 코드에 넣음·미배포)**: `/telegram/send` 본문 `hard:true`(1·"1"·"true"·"yes") 가 ⛔·🚨 와 같이 음소거를 뚫는다(«음소거라도 올리기» — 매크로 1.1.1006 강제 알람). `/telegram/photo` 에 음소거 확인을 ★처음 넣음★ — `expect_reply=1`(캡차)·⛔/🚨 캡션·폼 `hard=1` 은 늘 나가고(«캡차 항상 올리기»), 나머지 사진은 음소거면 200 muted(예전엔 확인이 없어 우연히 다 나갔다; 매크로 사진 호출부는 캡차 하나뿐). 생략 응답·로그는 `_tg_mute_skip` 한 곳. 차단 테넌트는 hard 로 못 뚫음(⛔ 정지 안내만, 사진 403) — 아이온2 권고. 계약 `updater/CONTRACTS_대시보드.md` §7 · CONTRACTS_아이온2 C4-c 요청은 `SHARED_ISSUES_대시보드.md`. 시험 `tests/test_tg_hard.py` 15 · 변이 7/7(기준선 초록 확인).
- **OCR 3df229e 반증 후속 (2026-09-24 아이온2 BLOCKER 1 + 2건, 코드에 넣음·미배포)**: ① `test_integration_contracts` I7 — 팜뷰 `ocr.py` 가 팜뷰 저장소에 **커밋 안 돼 있으면**(git ls-files) 글자 대조를 건너뛰고 `[SKIP]` 을 찍고 서버 쪽 고정 표(`_FV_OCR_ROUTES` 8길)만 본다 — 깨끗한 체크아웃에서 빨갛던 것. ② `ocr_label.ensure_tables` 루프마다 잠금(`_INIT_LOCKS`, `_lock` 과 따로) + 잠금 안 재확인 + ALTER «duplicate column» 무시 — 옛 표(/data) 위 첫 요청 8개 동시에 500 이 나던 것(시험 M-5·M-6). ③ 자동 씨앗은 첫 조회에서 `SEED_FIRST_WAIT_S`(3초)까지만 기다리고 나머지는 뒤 과제(`_SEED_TASKS`)로 — 2,000장 34.8초 > 팜뷰 10초(시험 S-15~15d). 변이 3/3 죽음. CONTRACT_OCR §3-b 에 알려진 틈 둘(씨앗 그림 prompt_sha1 없음 · 배포 전 라벨 lts NULL).
- **#114 B-DB10 서버 관리 설정 키 보호 (2026-09-24 아이온2 «나머지 66건», 코드에 넣음·미배포)**: `POST /setting/{key}`(세션, 100자 절단)가 서버가 통째로 쓰는 키를 덮을 수 있었다 — retired_pcs 를 한 번 쓰면 메모리는 그대로인데 저장본이 잘려 ★다음 재시작에 은퇴 PC 가 되살아남★. `main.SERVER_MANAGED_SETTINGS`(retired_pcs·no_account_pcs·abyss_acc_all·corridor_prog_all·lan_cache_last·acct_rotate(.broken)·tg_poller_lease·tg_offset·rot_allow·parsec_map) → 400, GET 은 그대로. lan_seed(시드 `seed_server.py`)·대시보드 키(sale_price·rental_kill·awakening_preset·ai_*·parsec_id/pw)는 그대로. 시험 `tests/test_setting_reserved.py` 7 — S-3 이 main.py 의 `set_setting(` 직접 호출 키(상수·ns 포함)를 읽어 목록과 대조(새 키 누락 방지). 목록 출처 = 이 방 B 반증 `scratchpad/b/findings_r1.md`(대부분 v4 9f27627 에서 닫힘) · 남은 것: CQ5(업데이터 큐+팜뷰 큐 이중 재시작) 계약 제안 `fix_tgcq/PROPOSALS.md` · TG7·CQ1 은 lc 쪽 → 아이온2.
- **#172 후속 대시보드 목소리 + #82 악몽 티켓 상한 (2026-09-24 아이온2, 코드에 넣음·미배포)**: ① 대시보드 `speak`·`speakLocal` 맨 앞 `text=ttsText(text)` — 팜뷰 ui/index.html 의 `ttsSino`·`ttsText` 를 글자 그대로 옮김(2번→이 번 · PC-22c→이십이 번 씨 · 계정2번 띄움 · 43,000번·2번호 그대로), 화면 글자는 그대로. 통합 시험 `test_integration_contracts` I8: 두 JS 글자 동일 + 대시보드 JS 답 = 팜뷰 `alarmvoice.speak_text` 답(28개) + 말하는 자리는 speak·speakLocal 뿐 · 변이 5/5. ② `NIGHTMARE_TICKET_MAX = 14, NIGHTMARE_TICKET_WARN = 12` — 12·13 호박색(`nmTicketWarn`), 14↑ 빨강·행 빨강, 넘는 실측(16)은 「16/14」 그대로. 리셋 5시는 미확인 가정(표시가 안 씀). 시험 test_summary JS-11~11e · test_js_b JS10-nm·nm2 · 변이 5/5.
- **#108 OCR 두 형식 — 서버가 매크로 형식도 받는다 (2026-09-24 아이온2 결정 «더하기만», 코드에 넣음·미배포 — push 는 아이온2 반증 뒤)**: 배포된 서버는 dhash 16자리만 받아 매크로(lc/ocr_label.py, dhash 64자리)의 제출이 전부 400 이었고 `/ocr/labels` 모양(열쇠·커서)도 달랐다. 더한 것 = ① `/ocr/submit` dhash 16·64자리 둘 다(`dhash_bits`), 길이 다르면 안 묶임, 256비트는 같은 값만(`OCR_NEAR_MAX_256`=0 — 실측 전) ② 매크로 칸 `site_raw`·`prompt_sha1`·`csha1`(=sha1(img_b64 문자열))·`phash` 저장(틀려도 빈칸, 거부 없음) — 저장 키는 site_safe 그대로라 `a.py:f`·`a_py_f` 가 같은 줄 ③ `/ocr/labels` 형식 B: since 에 `.`/`e` 또는 `mode=ts` → `{labels:[{site 원문, prompt_sha1, sha1, label, ts, deleted, dhash, phash}]}` — 대표 한 장만, bad=`bad image`, 되돌림=deleted, ts = `_next_lts`(테넌트마다 늘 증가), 쪽 경계에서 같은 ts 안 갈림. 커서 방식(정수 since·epoch)은 그대로. 표는 ALTER 로 열 여섯. 정본 = `CONTRACT_OCR.md` 하나(§2·§3-b). 시험 `tests/test_ocr_dual.py` 62 — ★매크로 lc/ocr_label.py 를 그대로 불러★ `_send`→200→팜뷰 라벨→`poll_now`→`before()` 적중·bad·undo 왕복 · 변이 22/22 죽음. `test_ocr_label` I1-e: `since=1.5` 는 이제 400 이 아니라 형식 B(의도된 변경). 알려진 틈: 같은 그림·다른 프롬프트는 처음 열쇠만 · 매크로 LABEL_MAX 64 < 서버 200. · ★프롬프트 예시값 교체(매크로 1.1.1006) 옛↔새 prompt_sha1 20쌍 별칭(`PROMPT_SHA1_OLD_NEW`) — ts 행을 두 열쇠로, 시험 A-1~6 · 변이 4/4★.
- **#125·#108 OCR 판별 — /bugs 씨앗 (2026-09-24 아이온2 요청, 코드에 넣음·미배포 — push 는 아이온2 반증 뒤)**: 서버 OCR(큐·라벨·/api/fv/ocr/*·/ocr/label)은 v4 9f27627 에 이미 배포돼 있었다(라우터가 `ocr_label.py` 라 main.py grep 0건). 더한 것 = 매크로가 예전부터 /bugs 로 올리던 `ocrdiff_*`·`oddfail_*` 크롭을 큐로 (`ocr_label.seed_parse`·`seed_one`·`seed_bugs_core`) — 길 셋: `POST /ocr/seed_bugs`(세션)·`POST /api/fv/ocr/seed_bugs`(팜뷰) · 큐 첫 조회 때 자동(`_auto_seed`, 프로세스·테넌트마다 한 번) · `main.upload_bug` 훅(`seed_upload_hook`, 실패 무시). `/ocr/submit` 저장 부분을 `submit_core` 로 떼어 같은 길(동작 그대로 — test_ocr_label 268 그대로). dHash 는 Pillow 가 없어 순수 파이썬(`_png_gray` 8비트 회색·RGB·RGBA·팔레트, 필터 0~4, 픽셀 상한 `SEED_MAX_PIXELS` · `png_dhash`). site: `ocrdiff_<site>_L…_G…` · `oddfail_narrow`→odd_energy · `oddfail_wide`→odd_energy_wide · 힌트는 파일 이름에서 되읽음(`_untag`). 시험 `tests/test_ocr_seed.py` 40 (독립 PNG 인코더로 필터 다섯) · test_contracts `t_fv_ocr` +5 · test_integration_contracts I7(팜뷰 ocr.py 경로 = 서버 라우트) · 돌연변이 17 중 16 KILLED + 1 등가(Paeth 동률 a≠b 면 pc=0 이라 c 가 이긴다 — 결과가 같다). ★막힌 것★: 매크로 lc/ocr_label.py(1.1.1004 WIP)는 dhash 256비트·(site,prompt_sha1,sha1) 열쇠·since=ts 라 서버 계약(64비트·epoch 커서)과 어긋난다 → 매크로 제출 전부 400 — 아이온2 에 올림(어느 쪽에 맞출지).
- **주인님 «빠뜨린 것» 감사(2026-09-24, 아이온2 결정 — `web/.claude/ops/owner_audit_20260924.md`)**: ① #114 첫 화면 `GET /` gzip(요청이 gzip 을 받을 때만, 압축본은 HTML 객체가 같으면 재사용 `main._html_gz`) + `Server-Timing: app;dur=<ms>` (`/`·`/status`·`/summary`, `main._server_timing`) — 운영 전후는 아이온2 가 로그인 세션으로 잰다. 로컬 전후(42ceca6 vs 13d0013): 서버 응답은 오차 수준, HTML 473→544KB(tw.css 인라인) → gzip 약 170KB. 도구 `scratchpad/http_ms/`. ② #64 팜뷰 업데이트 큐 T1 «[업데이트큐] 팜뷰가 가져감»(처음 줄 때 한 번, `v["t1_logged"]`)·T2 «[업데이트큐] 팜뷰 ack» 를 그 PC 로그에(`main._updq_log`·`_updq_age`) — 발사(쉬는 PC 한 대 왕복)는 주인님 지목 뒤(A7). ③ #124 `[알람]` 줄 끝 « (⏱ 발생→수신 X초 · 수신→전송 Y초 · 텔레그램 HH:MM:SS)» (`main._alarm_timing`) — t0 는 매크로 본문/폼 `t0`(프로그램방 1.1.1006), 텔레그램 응답 date 는 `_TG_RES`(ContextVar, 부른 과제에만 — 시험 가짜 `tg_send_text(chat, text)` 모양 그대로). ④ #82 악몽 +2 는 상한 N 을 주인님께 여쭘(아이온2). 시험 `tests/test_audit_0924.py` 16 · `test_alarm_event.py` 22(A-9b 팜뷰 실물 alarmvoice 로 말 그대로) · 돌연변이 10종 KILLED. FV_API «2026-09-24 추가 — 업데이트 큐 T1·T2 로그» + [알람] 지연 꼬리(두 사본).
- **#128 코드 넣음(미배포, 아이온2 승인 «2초 정착 그대로»)**: `main._alarm_event` — `/telegram/send`·`/telegram/photo` 가 실제로 보낼 때 `[알람] PC | 본문` 로그(음소거 생략분 제외, 전송 전), FV_API «밤3» 절, `tests/test_alarm_event.py` 9. 팜뷰 `alarmvoice.pick/summarize` 로 실제 파싱 확인. 상세 `src/SHARED_ISSUES_팜뷰.md` #128.
- **다음 묶음(스크래치, src 밖)**: #104 어비스 = `scratchpad/abyss2`(반증 9건 고침, 106/106, `PATCH_v2.diff`) · #108/#125 OCR = `scratchpad/build_ocr`(반증 10건 고침, 260/260). 계약: `FV_API.md` «2026-09-23 (밤) 추가» A~D(D 는 «다음 묶음» 표시).
- 그다음: 마스터플랜(`web/.claude/ops/MASTER_PLAN_20260923.md`) P0-12 · P1-43(음소거 영속 + 🛑 예외) · 진행도 정체 알람을 텔레그램으로 · P1-44~52 · P1-73.

## 이번에 고친 것 (2026-09-10 ~ 09-13, 전부 배포됨 또는 이번 커밋)

### 09-13 — 팜뷰 «팔린 만큼 줄인다» (주인님 지시, 팜뷰 세션 요청)
| 무엇 | 자리 |
|---|---|
| `POST /api/fv/kina_adjust` — 매니아 거래 1건만큼 `char_info.total_kina` 차감, `why.tid` 로 멱등(`dup:true`), 바닥 0, 그 PC 로그줄 + 스냅샷 캐시 무효화. 대시보드 `char_info` 프레임은 ★안 쏜다★(캐릭 목록을 지우고 「정보수집 완료」 토스트가 뜬다) | `fv_kina_adjust`·`_fv_kina_body`·`database.adjust_char_kina`·표 `kina_adjust` |
| `progress.kina_age_s`(정수, 모름 10^9) | `_fv_age_int`·`_fv_pc_view` |
| 계약 시험 19건 + 되돌리기 증명 3건 | `tests/test_contracts.py t_fv_kina_adjust`·`prove_guards.py` |
| ★실물 검증★ 2026-09-13 PC-07: -1000만(tid TEST-0913) → dup → +1000만(restore) 원상, `/status` `_total_kina`·PC-07 로그 두 줄·`collected_at` 불변을 서버 원본으로 확인. 표 `kina_adjust` 에 시험 2행이 남아 있다(값은 원상). `won` 없으면 로그가 «?원»(의도) | — |

### 09-12 (이번 커밋) — 1단계 20건 + 2단계 정리
| 등급 | 무엇 | 자리 |
|---|---|---|
| 치명 | `/img` 가 무인증인데 동기 httpx 3상류×15초로 서버 전체를 세웠다 → 스레드 + 매니페스트 밖 이름 404 + LRU(통째 clear 제거) | `serve_image`·`_fetch_image_upstream` |
| 치명 | `version.json` 갱신이 동기 httpx(최악 14초)였고 `push_state` 경로에서 불렸다 → `_load_version_json_async`(스레드+잠금+이중확인) | 호출부 4곳 |
| 치명 | 렌탈 `/updater.exe` 가 75MB 동기 다운로드+압축(최악 3분) → 스레드 | `download_updater` |
| 심각 | 사람 표식 `_by` 가 첫 WS 배달에만 붙고 DB 엔 없어 ★재배달이 「사람 아님」★ 으로 갔다(사고 523 재현) → DB 에도 저장, 화면엔 `_public_args` 로 숨김 | `_dispatch_macro_command`·`_strip_cmds` |
| 심각 | `/updater/command/{pc}` 에 A7 브로드캐스트 가드가 0줄 | 라우트 |
| 심각 | 텔레그램 음소거 키가 pc_id 뿐이라 테넌트가 섞였다 → `_tg_muted(tenant, pc)` | `_TG_MUTE` 3곳 |
| 심각 | `/setting/{key}` API 키 조회가 부정목록(비밀 2개 제외 전부) → 허용목록 `MACRO_READABLE_SETTINGS` | `get_setting_ep` |
| 심각 | 버그스샷 목록이 481×getsize 를 매번(팜뷰 20초 폴링) → `_bug_scan` 캐시 + scandir | `_list_bug_files` |
| 심각 | `_rot_stop` 이 옛 무장으로 새 무장을 지울 수 있었다(독스트링은 「await 없다」고 했지만 5곳이 await 뒤) → `_ROT_STEP_CUR` 로 자원 쪽에서 확인 | `_rot_stop`·엔진 루프 |
| 심각 | 엔진이 단계 예외를 삼켜 ★상한 알람도 같이 죽었다★ → `err_n` 세고 3틱째 🚨 | `_rot_engine` |
| 심각 | `_rot_load` 복원 실패가 stdout 뿐, 깨진 저장본을 다음 save 가 덮음 → `.broken` 보관 + 텔레그램 | `_rot_load` |
| 심각 | FV `slots_done` 이 `today` 플래그를 안 봐 어제 완주를 오늘로 · `totals.bugs` 카드 합산 3배 · events 같은 초 유실 · 일괄 명령 1대 성공도 `ok` | `_fv_pc_view`·`_fv_build_snapshot`·`fv_events`·`fv_command_send` |
| 심각 | `/check/purge` 아무 테넌트 키 · FV 토큰 추측 무제한 | `check_purge`·`_fv_guard` |
| 경미 | JS `armed` 거부 표시가 start 만 · `_rot_say` 가 보내기 전에 「전송」 기록 + ⛔ 화면 배너 없음 · 재무장 시 옛 해제 사유 잔재 · 내부망 주소 캐시 재배포 소실 | `sendCmd`·`_rot_say`·`_rot_arm`·`_lan_cache_*` |
| 정리 | 상수 5종, 인증 도우미 2종(46곳), `_attach_char_info`, 죽은 줄 1 | — |

### 09-11 (배포 `e8e2a75`·`339e6cc5`) — 전수조사 10축 확정분
저장형 XSS 8곳(`renderRow` 에 esc 0개) · N+1 제거(상태조립 70~106ms → 14ms) · `insert_log` 연결 2→1 · `logs(created_at)` 인덱스+정렬 · `get_pending_commands` 읽기 경로 쓰기락 제거 · 소켓 소유권(`send_command_to_macro`) · `/ws` finally · WS ack 이력 방송 · 음소거가 ⛔ 삼킴 · 로그 배치 유실 표시 · `ack_command` 상태가드 · 키나 0 덮어쓰기 · LIKE 이스케이프.

### 09-10 (배포 `d55be8c`~) — 명령이 느리던 진짜 원인
`macro_websocket` 의 finally 가 무조건 pop 이라 ★재접속한 새 연결을 옛 핸들러가 지웠다★. 서버만 「WS 없음」으로 믿어 폴링 15~60초를 기다렸다. 그 앞에 세운 가설 둘(「경로가 느리다」「루프가 굳는다」)은 계측이 깼다.

## 시도했다가 버린 접근과 이유
- **미러 CSS 를 「가장 큰 `<style>` 블록」으로 뽑기** — JS 주석 안의 `<style` 에서 시작한 4,898줄짜리 가짜를 집어 `dashboard.css` 를 한 번 망가뜨렸다. 진짜는 803줄. → 「함수가 있는가」로 가른다(`verify.py _looks_right`).
- **문자열 grep 으로 「고쳤는지」 검사** — 적대검증이 「진짜 가드를 지우고 주석에만 문구를 남긴 사본」을 20/20 으로 통과시켰다. → 시험은 전부 실호출, `prove_guards.py` 로 되돌려 확인.
- **인덱스만 달기(`logs(created_at)`)** — `ORDER BY id` 가 그대로면 SCAN 그대로(8.96→9.31ms). 정렬을 커서와 맞춰야 0.05ms.
- **`/parsec/map` POST 를 세션 전용으로** — 관제컴 `parsec_multi.py` 가 API 키로 부른다. 서버가 먼저 바꾸면 도구가 깨진다 → `CONTRACTS_대시보드.md` #2.
- **`set_slot_filter` 에 `_by` 달기** — 사람 표식이 붙으면 매크로가 진행 중 워커를 취소한다(`loot.py` `_is_human` 갈래). 설정 명령에 그걸 달면 슬롯 토글이 사냥을 끊는다. 안 달았다.
- **`main.py` 분할·static 서빙 전환** — 주인님 「아직 올리지 마라」 + 라이브 72카드. 미러는 만들어 두되 서빙은 안 바꿨다.

## 남은 TODO (주인님 결정 필요 순)
1. `commands`·`updater_commands` 표에 정리 규칙이 없다(단조 증가, 200k행에서 33ms). 보존 기간을 정해야 한다.
2. 업데이터 명령 만료·dedup(`SHARED_ISSUES` #6) — 「꺼진 PC 가 켜지면 밀린 업데이트를 받는다」가 의도인지.
3. `_rot_step_pc` `starting` 7분 상한이 캡차·재연결을 못 넘긴다(`switching` 은 20분) — 줄기.
4. 재무장이 `tvisit` 을 초기화해 full 순환이 끝낸 계정을 다시 돈다 — 줄기.
5. 단계 전이 절반이 즉시 저장되지 않는다(재배포가 30초 창에 걸리면 옛 단계로 복원).
6. `build_full_state` 합치기(coalesce) — N+1 제거 뒤 14ms 라 급하지 않다. 재고 판단.
7. FV `raw`(응답의 53%, PII) 제거 — `CONTRACTS` #4.
8. `secret_audit` 빨간불(exe 안 비밀 11개) — 주인님 몫.

## 함정 포인트
- **`main.py` 는 LF, `database.py` 는 CRLF.** 치환 스크립트에서 줄바꿈을 한 값으로 뭉뚱그리면 조용히 count=0. 파일마다 잰다.
- **`servercode.py`(web/.claude/ops)가 배포 확인의 정본** — 작업트리 sha256 은 CRLF 로 틀린다. 커밋된 바이트로 잰다.
- **`_rot_engine` 은 부팅 뒤 `ROT_BOOT_GRACE_S`(25초)를 잔다** — 시험은 0 으로 놓는다.
- **매크로는 실행 전에 ack 한다.** 서버 `acked` = 「받았다」지 「했다」가 아니다. 유실·거부는 아직 서버에 안 보인다(`CONTRACTS` #1).
- **한 PC = 한 매크로 = WS 하나.** 같은 pc_id 재접속은 옛 것을 `close(1012)` 로 밀어낸다. 이걸 「연결이 자꾸 끊긴다」로 읽지 마라.
- **`/status` 의 base 카드는 업데이터 기록이 없으면 `offline`** — 시험에서 status 를 볼 땐 `upsert_updater_status` 를 같이 심는다.
- **FV `pcs` 는 dict.** 배열로 순회하면 키 문자열이 나온다.
- **다른 세션이 같은 repo 에 커밋한다** — 작업트리에 `client/updater.py`·`seed_server.py` 가 수정된 채 있었다(내 것 아님). `git add .` 금지, 파일을 골라 스테이징. 빌드 잠금(`web/.claude/ops/room.py lock build`)이 있어도 겹친 적이 있다(2026-09-10).
- **다른 손이 `main.py` 를 고치면 static/ 미러가 낡는다** — `verify.py` 4단계가 잡는다. `--sync-static` 으로 다시 뽑는다.

## 다른 영역에 넘긴 것 (요약 — 원문은 `../SHARED_ISSUES_대시보드.md` · `../CONTRACTS_대시보드.md`)
- **SHARED_ISSUES 7건**: 매크로가 실행 전 ack(유실 안 보임) · `telegram_send` 가 `ok:false` 를 안 봄 · HTTP 로그 폴백 51줄부터 유실 · `updater/server/main.py` 미러 낡음 · `parsec_multi.py` 가 API 키로 주소록 씀 · 업데이터 명령 만료 없음 · exe 안 비밀 11개.
- **CONTRACTS 5건**: ack 에 status/why 싣기 · 파섹 주소록 쓰기 인증 분리 · FV `pc:"all"` 서버 거부 · FV `raw` 제거 · FV 값 뜻 정정(`slots_done`·`totals.bugs`·events 잘림 — 이번에 서버가 고침, 팜뷰가 알아야 함).
